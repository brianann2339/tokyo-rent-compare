node_modules/
.DS_Store
*.log

# 抓下來的原始 HTML：體積大（單次數百 MB），不進 git
data/raw/
# conditional GET 的 ETag / Last-Modified 快取
data/cache/
# 建置產物
data/dist/
# 舊的未壓縮真相層（已改用 .ndjson.gz）
data/normalized/*.ndjson
web/public/data/
web/dist/
# 爬蟲／建置的暫存檔（寫完才 rename 成正式檔）——曾被 `git add -A` 誤收進版本庫
*.ndjson.gz.tmp
data/out/prov.tmp/
.scratch/
