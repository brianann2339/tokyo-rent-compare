# 覆蓋缺口調查：站方實際有多少 vs 我們抓到多少

產生於 2026-09-06。方法：每個來源一個調查員（讀 adapter → 查站方實際規模 → 對照我們的量 → 分辨漏抓 vs 正當差異），再由**獨立**複核員重算一次。合規：先讀 robots、單執行緒、間隔 ≥3 秒、不繞過任何防護；Sakura House 與 JKK 全程離線分析（0 次請求）。

## 結論一覽

| 來源 | 判定 | 站方 | 我們 | 可補 |
|---|---|---|---|---|
| 三站合併：socialapartment（Social  | **確認漏抓，可補** | 【Social Apartment】2026-09-06 GET https://www.social-apartmen | 指令 `gzip -dc data/normalized/{source}.ndjson.gz` 逐 | 棟數：Couverture 23 → 24（+2 正確棟、−1 誤收棟）。 |
| borderless（BORDERLESS HOUSE, | **確認漏抓，可補** | 站方自報（2026-09-06 實測）：**東京 44 棟、530 床**。取得方式：站內搜尋頁 `/jp/shareh | `gzip -dc data/normalized/borderless.ndjson.gz | w | 物件數：**+0 棟 / +0 間**（本來就沒漏）。 |
| hituji（ひつじ不動産 / https://www. | **確認漏抓，可補** | **1,240 棟（2026-09-06 站方自報）**，其中 **387 棟有空房、853 棟満室**。 | 三層數字不同，必須分清楚： | 以 2026-08-16 同一份快照（可精確驗算的唯一基準）計： |
| tokyosharehouse（東京シェアハウス, ht | **確認漏抓，可補** | 站方沒有公布「東京都 N 件」這個數字，所以分兩層說： | normalized 層：1,068 棟東京建物 / 898 間房（指令：解 data/normal | 修法 A：+6 棟東京建物、+7 間房（4070:2、4239:2、4366:1空室+2空室予定；4320／3107／4 |
| oakhouse（Oak House／オークハウス, h | **確認漏抓，可補** | 取得方式：GET https://www.oakhouse.jp/area/tokyo （2026-09-06，HTTP | 我自己從 `data/normalized/oakhouse.ndjson.gz` 算出（gunzi | **主修（article 版型）**：+449 棟 / +1,017 間空室房（東京）。索引口徑 51 棟 → 約 50 |
| 新來源盤點（非既有 12 來源的覆蓋缺口）：實測 18  | **確認漏抓，可補** | 各候選站方自報東京數（全部為 2026-09-06 我實際抓頁面讀到的字面數字）： | 目前 12 個來源合計（依你給的 2026-09-06 索引表相加）：棟 30,863 / 間 70 | 分階段（皆為站方刊登量，重疊未扣，見 caveats）： |
| sakurahouse | 正當差異（不是漏抓） | **東京棟數：92 棟（站方自報 95 棟全站，扣掉 3 棟京都）。東京房間數：922 間（站方 payload 自算） | **92 棟 / 666 間**（我自己算的，非沿用交辦單）。 | **補齊覆蓋缺口的預期收益：0 棟 / 0 間。** 東京 92 棟已 100% 枚舉，沒有可補的東西。 |
| ur（UR賃貸住宅 / 都市再生機構） | 正當差異（不是漏抓） | **308 是「建物數」，不是房間數——而且是東京全部 danchi 目錄數（含目前 0 空室者），不是空室物件數。** | **69 棟 / 150 間**（2026-08-16 抓取快照）。 | **0 間。** 沒有可補的房間——站方今天全東京只有 119 間空室，我們手上的 150 間已經涵蓋了抓取當日站方的全 |
| jkk（JKK東京／東京都住宅供給公社） | 正當差異（不是漏抓） | 今日（2026-09-06）的站方數字**取不到**——本任務最後一條禁止實際請求該站，我因此對 to-kousya.o | 1 棟 / 7 間。 | **不適用於主缺口**（主缺口 0 戸可補）。 |
| leopalace21（レオパレス21，https:// | 沒有缺口 | **2,769 件（＝房間數，非棟數），2026-09-06 實測。** | **1,699 棟 / 2,881 間**（真相層 `data/normalized/leopala | 0 棟 / 0 間。今日站方東京只公開 1,694 棟 / 2,769 間，我們手上是 1,699 棟 / 2,881  |

## 逐來源細節

### 三站合併：socialapartment（Social Apartment）／couverture（Couverture）／villagehouse（Village House） — **確認漏抓，可補**

**站方自報**

【Social Apartment】2026-09-06 GET https://www.social-apartment.com/builds（HTTP 200，304,619 bytes）。用 adapter 同一條 regex 數 href="/builds/{pref}/{city}/view/{id}"：全國 48 棟（去重後），其中 tokyo 31 棟。同頁每張卡內嵌房間卡（class="building-card-room-list-component__label"）：東京 31 張卡中 16 張含「満室」字樣、15 張有房間卡，房間卡合計 22 間。（先前記錄「全國 49 棟」→ 今日 48 棟，少 1 棟。）

【Couverture】2026-09-06 GET https://couverture.jp/map.html（HTTP 200，32,686 bytes）。用 adapter 同一條 regex 數 house_detail/([a-z0-9-]+)/ 去重＝25 個物件。站方頁面本身沒有「全◯件」的自報數字，25 是我自己數的連結數。

【Village House】2026-09-06 兩個獨立來源互相印證：
(1) GET https://www.villagehouse.jp/VillageHouse_Sitemap/sitemap_property_page_map_jp.xml（HTTP 200，160,129 bytes）＝1,064 個 /chintai/ 物件 URL（全國），其中 /tokyo/ 段者恰 7 個。
(2) GET https://www.villagehouse.jp/chintai/kanto/tokyo/（HTTP 200）站方自報「該当件数: 7」，卡片正是同樣那 7 棟，其中 3 棟標「空室なし」（芝浦タワー、小比企、潮見タワー），4 棟有空室。

**我們的量**

指令 `gzip -dc data/normalized/{source}.ndjson.gz` 逐行 JSON.parse 自算（2026-09-06 執行）：

【Social Apartment】normalized 共 31 棟（東京），其中 14 棟有 units、units 合計 22 間。索引表的「14 棟 / 22 間」＝有空室的棟數，另 17 棟滿室棟其實也在 normalized 裡。
【Couverture】normalized 共 23 棟，其中 9 棟有 units、units 合計 19 間。但 data/raw/couverture 有 26 個 .html.gz（＝25 個物件頁 ＋ 1 個 map.html），也就是抓回來 25 頁、只落地 23 棟。
【Village House】normalized 共 7 棟，其中 4 棟有 units、units 合計 16 間。索引表的「4 棟 / 16 間」＝有空室的棟數。

**成因**

【Social Apartment：no-gap，正當差異】
discover 走 /builds 一頁列完，無分頁（頁面出現 96 次 "pagination" 全屬圖片輪播 swiper，無 page= 參數、無「もっと見る」）。滿室棟照收（我們 31 棟中 17 棟 units=0）。索引表的 14 棟只是「有空室的棟」，把它跟站方 31 棟相比才看起來像缺口，實際不是。棟內房間數的逐棟差異（例：view/85 站上 317、我們 211+317；view/63 站上満室、我們仍有 606/1508）是 2026-08-16 → 2026-09-06 三週的空室異動，非系統性漏抓。已知的初期費用不刊登＝notOffered，與覆蓋率無關，確認無誤。

【Couverture：under-crawl，extract 丟棄】
discover 沒問題（map.html 25 個 slug 全部枚舉到，且 2026-08-16 存檔的 map.html 同樣是 25 個），25 頁也都實際抓回並存在 data/raw。問題出在 extract：`crawler/sources/couverture/index.ts:203` 的住所 regex 要求「(?:東京都)? + 非貪婪 1–6 字 + 区或市」，而 kitaotsuka2（クーベルチュール北大塚Ⅱ）與 kitaotsuka3（クーベルチュール北大塚THREE）兩頁的住所是站方打錯的「東京都豊島北大塚3丁目」——少了「区」字。regex 匹配失敗 → 第 204 行 `return null` → 整棟（含 13 間、15 間房間資料）被丟掉，而且丟得無聲無息。
同一條 regex 還有反向缺陷：它沒有都道府県守門，`埼玉県川口市芝樋ノ爪` 會被非貪婪吃成 ward=「埼玉県川口市」而通過，於是 COCONわらび（實際在埼玉県川口市）被寫成 prefecture=東京都、addressRaw=「東京都埼玉県川口市芝樋ノ爪」收進東京資料集。它目前 units=0 所以沒污染索引數字，但只要該棟釋出空房就會混進來。

【Village House：no-gap，正當差異】
robots 禁 /api/，adapter 照規矩走 sitemap，而 sitemap 的東京 7 筆與站方搜尋頁自報的「該当件数: 7」完全一致，7 個 URL 逐筆相同。我們 units=0 的 3 棟，正是站方今天標「空室なし」的那 3 棟——「4 棟」是空室現況，不是漏抓。反向成本也沒漏：16 間 units 全數帶有火災保険、退去時クリーニング費用、短期解約違約金三個欄位（16/16）。

**修法**

只需改 Couverture 一處：`/Users/shrape/Desktop/Codex/東京租屋查詢/crawler/sources/couverture/index.ts:202-204`（連帶 :221 addressRaw、:223 ward 的取值來源）。SA 與 VH 不需要改。

做法（兩件事一起做，因為是同一條 regex 的一體兩面）：
1. **加都道府県守門**：先取整段住所原文 `const addrRaw = /住所｜\s*｜?\s*([^｜]{1,40})/.exec(t)?.[1]?.trim()`；若 addrRaw 以「東京都」以外的都道府県開頭（`/^(?!東京都)[^\s]{2,4}[県府道]/`）→ 明確 `return null`（排除 COCONわらび）。
2. **住所缺「区」時用同頁證據補 ward，而不是整棟丟掉**：先以 `/^(?:東京都)?([^\s0-9]{1,5}?[区市])/` 從住所取 ward；取不到時，退而從**同一頁**的 `<title>` / `<meta name="keywords">`（前 4,000 字元）取第一個 `([一-龥ぁ-ん]{1,4}区)`。北大塚Ⅱ／THREE 兩頁的 title 是「…北大塚Ⅱ・大塚・巣鴨・**豊島区**・湘南新宿ライン・池袋」，ward=豊島区 有同頁出處、不是推測；addressRaw 仍原文照存「東京都豊島北大塚3丁目」（站方原文有誤就照實留，不要替站方補字）。兩者都取不到才 `return null`，並且該路徑應記進健康報告而非靜默丟棄。

**回歸驗證（我已對 25 份存檔 HTML 實跑過提案邏輯，未改動任何檔案）**：現行 23 棟 → 提案 24 棟；其中 22 棟 ward 與現行**逐棟完全相同**，+2 棟為北大塚Ⅱ／THREE（ward=豊島区，來源 title/keywords），−1 棟為 COCONわらび（正確排除）。

另建議（非本次缺口，但同一支 adapter 的既有小瑕疵）：`name` 取自 `<title>` 時未做 HTML entity 解碼，導致既有的 kinshicho2 存成「クーベルチュール錦糸町&#8545;」，修好後北大塚Ⅱ 會有同樣問題。

**預期增加**

棟數：Couverture 23 → 24（+2 正確棟、−1 誤收棟）。
間數：以 2026-09-06 現況為 **+0 間**——北大塚Ⅱ（13 間）與北大塚THREE（15 間）今天 100% 満室，而 adapter 只輸出空室；COCONわらび 本來就 0 間，所以索引的「9 棟 / 19 間」不會立刻變大。
實質收益在三處：(a) 這 2 棟的建物層資料（totalUnits 13／15、車站、契約手数料規則）進得來，往後任何一次 crawl 只要有空房就會自動被收；(b) 消除 1 筆把埼玉県物件標成東京都的髒資料；(c) 把「整棟靜默丟棄」變成可觀測，避免同類站方錯字再無聲吃掉物件。
SA 與 VH 修無可修，預期增益 0。

**合規記錄**：先讀 robots，全部在打列表／物件頁之前完成（2026-09-06 實測）：
- https://www.social-apartment.com/robots.txt → HTTP 200，全文 99 bytes 只有一行註解「# See https://www.robotstxt.org/robotstxt.html …」，無 User-agent、無 Disallow。/builds 與 /builds/tokyo/... 未被禁。
- https://couverture.jp/robots.txt → HTTP 404（回傳站內 404 HTML，title 為「404page Not found…」）＝全站無 robots.txt，無任何 Disallow。/map.html、/house_detail/ 未被禁。
- https://www.villagehouse.jp/robots.txt → HTTP 200，890 bytes；`User-agent: *` 群組 Disallow 含 /api/、/ajax/、/sitecore/、/application-form/、/wp-admin/、/message/*、/thank-you/*、/gomilink/、/valuecommerce-bridge/、/*?utm_*=*，並宣告 `Sitemap: 

**異常與不確定**：1. Social Apartment 的 22 間是「/builds 卡片上的房間卡數」。全國 48 張卡的每卡房間數分布為 {0:27, 1:9, 2:12}，最大值恰為 2，因此**無法排除站方在列表卡片上截斷到 2 間**。我用兩筆明細頁抽驗（view/11、view/17，兩者列表與明細皆為 2 間）沒看到截斷跡象，但抽驗只有 2 筆、且兩筆的值都恰好是 2，證據力有限——嚴格說 22 應視為**下界**。要確定實數需逐一打 15 個有空室的明細頁，超出本次「最多十幾頁」的額度，未做。
2. 我們的 normalized 是 2026-08-16（SA、CV、VH 皆是）的快照，站方數字是 2026-09-06 現況，兩者相隔三週。棟層的比對是同集合比對（可靠）；間層的比對必然混入空室異動，所以本報告的所有「缺口」判定都建立在**棟層**，沒有把間層差異當成漏抓。VH 今天東京空室已降到約 9 間以上（郷地 ≥3、秋多 2、向台タワー ≥3、品川八潮タワー 1；「空室あり」的卡片最多只顯示 3 間、超過才不列數字，所以是下界），少於我們快照的 16 間——這是反向的異動，同樣不是漏抓。
3. Couverture 站方沒有任何「全◯件」自報數字，25 這個數是我自己數 map.html 的連結。若站方另有不在 map.html 上的物件，本次無從發現；sitemap.xml 依既有記錄停在 2019 且更不完整，我沒有再打它。
4. 提案的 ward fallback（從 title/keywords 取「◯◯区」）我只在 25 份存檔上驗過，結果是 22 棟不變、+2、−1；但這條 fallback 依賴站方 title 的寫法，若站方改版 title 就會失效——它是「取不到才用」的第二順位，不會影響現行 22 棟。
5. 「北大塚Ⅱ／THREE 今天 100

### borderless（BORDERLESS HOUSE, https://www.borderless-house.com / .jp 為同一站） — **確認漏抓，可補**

**站方自報**

站方自報（2026-09-06 實測）：**東京 44 棟、530 床**。取得方式：站內搜尋頁 `/jp/sharehouse/` 的 JS（`function changeParamater()`）用 `$.get('/jp/sharehouse/searchplan/?'+myLocation+'&usequery=true')` 取 JSON；我用同一組參數打 `GET https://www.borderless-house.com/jp/sharehouse/searchplan/?&region=jp&v=g&show-full-houses=true&usequery=true`（HTTP 200, application/json, 3,056,182 bytes），回應中 `dispDatas` 長度 = 44、`housePlans` 長度 = 530、`checkRegionTokyo: true`。`region=jp`＝東京（同一段 JS：`$('#regionTokyo').prop('checked') ? '&region=jp' : ''`；`region=osaka`＝関西、`region=sendai`＝仙台），且回傳的 44 筆 `address` 全部以 Tokyo 開頭，可自證是東京。
只算「有空房」（不加 `show-full-houses`）：`dispDatas` = 32 棟、`housePlans` = 61 床（同端點，1,388,102 bytes）。
sitemap 交叉驗證：`GET /sitemap.xml`（200、410 筆 `<loc>`）中 `/jp/sharehouse/` 共 58 筆＝1 索引頁＋57 物件（東京 44＋関西 12＋仙台 1），東京 slug 集合與上面 JSON 的 44 筆完全相同。

**我們的量**

`gzip -dc data/normalized/borderless.ndjson.gz | wc -l` → **44 棟**（全部 `prefecture=東京都`），各棟 `totalUnits` 加總 = **533 床**；其中 `units` 陣列非空者 = **30 棟**、`units` 總數 = **56 間**。
索引表上的「30 棟 / 56 間」就是後面兩個數：`crawler/src/cli/build-data.ts:369` 的 `if (units.length === 0) { emptyBuildings += 1; continue; }` 把 0 間房的 14 棟丟掉。
`ward` 欄：44 棟全部為空字串（腳本輸出 `distinct ward values: {''}`），對應 `crawler/sources/borderless/index.ts:401` 的 `ward: ''`。

**成因**

要分成兩件事，結論相反：

**(1) 物件覆蓋：沒有缺口（no-gap）。** 我們的 44 棟 = 站方今天的 44 棟，slug 逐筆比對零差異（site-only=[]、ours-only=[]）；床數 44 棟中 43 棟完全一致，只有 `shinjuku_chuo` 我們 6、站方 3（站方該棟 `updatedAt` 為 2026-09-05，是 8/16→9/6 之間站方自己減了 3 床，不是解析漏抓）。索引顯示 30 棟 / 56 間是**兩層正當篩選**疊加：`index.ts:341` 的 `.filter((r) => /^(open|will open)$/i.test(r.status))` 只收募集中的床（Occupied 不收），加上 build-data 丟掉 0 間房的棟。站方自己的預設檢視同樣只給 32 棟 / 61 床，口徑一致——share house 空室率低是真實情況，不是漏抓。

**(2) `ward` 欄位：是漏抓（under-crawl），而且是 44/44 全漏。** adapter 檔頭限制 1（`index.ts:12-17`）寫「全站不公開街道地址……所以 addressRaw 與 ward 一律留空」。這句話對**詳情頁 HTML** 成立（我在 2026-08-16 存的 roppongi 原始頁 grep：`東京都`=0、`区`=0、`Minato`=0、`address`=0、`zipCode`=0），但對**站方自己的搜尋 JSON 端點不成立**——同一個公開端點對每一棟都回傳完整住所與郵遞區號。當初只看詳情頁就下了「站方不公開地址」的結論，端點沒被發現，所以整個 `ward` 欄放棄了。這不是「反推」，是站方自報的事實欄位，取來填不違反禁止虛構的規則。

**修法**

**主修：在 discover 多打 1 個請求，把区經 `TargetRef.hint` 帶給 extract。**

1. `crawler/sources/borderless/index.ts:355-361`（`discover`）——在既有 sitemap 之後加一次 fetch：
```ts
const idx = await fetcher.get(
  `${SITE}/jp/sharehouse/searchplan/?&region=jp&v=g&show-full-houses=true&usequery=true`);
const byslug = new Map<string, { ward: string; zipCode: string }>();
for (const h of (JSON.parse(idx.body) as { dispDatas?: readonly Record<string, any>[] }).dispDatas ?? []) {
  const ja = String(h?.i18n?.ja?.address ?? '');
  const en = String(h?.address ?? '');
  const ward = wardOf(ja, en);              // 見第 2 點
  if (ward !== '') byslug.set(String(h.slug), { ward, zipCode: String(h.zipCode ?? '') });
}
if (byslug.size === 0) throw new Error('[borderless] searchplan 取不到任何東京物件的行政区，端點或版面可能已改');
for (const slug of slugs) yield { url: `${SITE}/jp/sharehouse/${slug}/`, hint: byslug.get(slug) };
```
2. 新增一個純函式 `wardOf(ja, en)`（放在 `parseRegion` 附近，比照現有 export 慣例便於單元測試）：先用 23 区字串在 `i18n.ja.address` 內比對；比不到再用英文 `address` 的 `/\b(Chiyoda|Chuo|Minato|…|Edogawa)-?\s?ku\b/i` 對映回中文区名；兩者都不中就回 `''`（維持現狀留空，不猜）。實測今天 44/44 都能由 ja 直接命中，英文那條只是防呆。
3. `index.ts:398-401`：把
```ts
// 全站不公開街道地址（見檔頭限制 1）——留空，不從站名或車站反推
addressRaw: '',
prefecture: '東京都',
ward: '',
```
改成
```ts
// 詳情頁 HTML 不印住所；行政区取自站方搜尋端點 searchplan 自報的 address/zipCode（非反推）
addressRaw: '',
prefecture: '東京都',
ward: (ref.hint as { ward?: string } | undefined)?.ward ?? '',
```
（`addressRaw` 建議**維持空字串**，理由見 caveats。）
4. 檔頭限制 1（`index.ts:12-17`）與 `manifest.legal.notes`（75-85 行）要一併改寫：把「全站不公開街道地址」修正為「詳情頁 HTML 不印住所，但站方搜尋端點 `/jp/sharehouse/searchplan/` 逐棟回傳 address／zipCode，本 adapter 只取其中的行政区」，並把 2026-09-06 的複查（robots 仍 404、sitemap 仍 58 筆 `/jp/sharehouse/`）記進去。`manifest.capabilities` 不需改（ward 不是 `Field`，不在 provides/neverProvides 清單裡）。

**順帶（可選，非必要）**：現在 discover 吐 57 個 slug、extract 再靠麵包屑丟掉 13 個非東京的，等於每次 crawl 白抓 13 個 200KB+ 的詳情頁。既然 `region=jp` 的 JSON 已經給出精確的 44 個東京 slug，可以改成只 yield `byslug.has(slug)` 的那些；`index.ts:366-370` 的麵包屑檢查建議**保留**當作第二道防線（端點若改版，麵包屑仍會擋下非東京物件）。

**預期增加**

物件數：**+0 棟 / +0 間**（本來就沒漏）。
欄位：`ward` 由 0/44 變 **44/44**，涵蓋 19 個区。以現行索引口徑，等於讓 30 棟 / 56 間（依今天站方狀態約 32 棟 / 61 間）從「区篩選下的空集合」變成可被篩到、也可被 `web/src/data.ts:383` 的自由文字搜尋命中——borderless 不再在比價站上半隱形。
成本：每次 crawl **+1 個請求**（約 3MB JSON），可選最佳化再省下 13 個詳情頁請求，淨請求數其實下降。

**合規記錄**：**先讀 robots**：第一、二個請求就是 `GET https://www.borderless-house.com/robots.txt` 與 `https://www.borderless-house.jp/robots.txt`，兩者皆 **HTTP 404**（回站內 404 HTML，110,167 bytes，兩台同一份），全站無 robots.txt、無任何 Disallow、無 Crawl-delay——與 adapter `legal.notes`（2026-08-16 查證）一致，今天複查仍成立。

**請求數與節奏**：共 **8 個請求**，單執行緒循序，每個間隔 `time.sleep(3.2)`（>3 秒），無平行。清單：robots(.com)、robots(.jp)、`/sitemap.xml`、`/jp/tokyo/`、`/jp/all/`、`/jp/sharehouse/?&region=jp&nationality=2&sort-field=monthlyRent`、`searchplan?...&show-full-houses=true`、`searchplan?...`（無 show-full-houses）。

**User-Agent**：照抄 `crawler/src/http.ts:20-22` 的 `USER_AGENT`

**異常與不確定**：1. **這是需要你裁決的一點，我沒有替你決定**：那份住所站方**公開回傳但不在任何頁面上顯示**——我確認過該站自己的 render JS 從頭到尾沒有印 `address` 或 `zipCode`（`sh_search.html` 內 `address` 命中 0 次、`zipCode` 0 次，只印 `areaTag.name` 與 `nearestStation1`），詳情頁 HTML 也完全沒有。端點本身無登入、無 cookie、無防護、無 robots 限制，是搜尋頁自己 `$.get()` 的同一支；但「技術上取得到」不等於「站方願意公開門牌」。我的建議是**只存到「区」為止，不要存番地與 `googleMapsUrl` 的經緯度**，`addressRaw` 維持空字串，欄位出處字串（srcText）寫端點 URL＋区名而不要寫整串住所。若你認為連区都不該用，那 borderless 就維持 ward 空白，我不會改用車站反推——證據 D 已證明反推至少會錯 3 棟。
2. **未重跑 crawl**。所有比較是「我們 2026-08-16 的 normalized」對「站方 2026-09-06 的即時 JSON」，中間 3 週站方有異動（各棟 `updatedAt` 落在 2026-08-28～09-05），`shinjuku_chuo` 6→3 床即為一例。要拿到真正同時點的差異必須重跑，但你的指示是不要跑 crawl，我沒跑。
3. **住所本身我沒有做外部查證**。我只驗到「站方自報值內部一致」：英文与日文住所的区 44/44 相符、郵遞區號前三碼與区自洽。我沒有拿地圖或郵政資料獨立核對任一筆門牌是否真實存在。
4. **`tosReviewed: 'pending'` 沒有改變**。我這次沒有另外去找利用規約頁；adapter 註記的「站內只找

### hituji（ひつじ不動産 / https://www.hituji.jp） — **確認漏抓，可補**

**站方自報**

**1,240 棟（2026-09-06 站方自報）**，其中 **387 棟有空房、853 棟満室**。

取得方式：`GET https://www.hituji.jp/comret/info/tokyo?page=1`（帶 `RSC: 1`），RSC payload 原文含
`"searchStates":[{"code":"tokyo","name":"東京","comretCount":1240,"towns":[{"code":"setagaya","name":"世田谷区","comretCount":109,...`
→ 東京都層級的 `comretCount` = 1240。

空房棟數：`GET .../comret/info/tokyo?page=48`（累積式分頁，`pageForCount(1240)=48`）回 2,143,950 bytes，用專案自己的 `parseSummaries()` 解出 **1,240 筆 summary**（與站方自報數完全相等，累積式分頁公式 30+26×(N−1) 今日仍成立），其中 `hasAvailableRoom===true` 者 387 筆、false 者 853 筆。

**房間數**：站方今日的列表 payload 已不再提供 `availableRoomCount`（見 caveats 的 schema 變更），今日無法直接加總。可加總的是我們 2026-08-16 抓的同一份列表存檔（`data/raw/hituji/ce8467dc0fbc06e9abc382a99ad976f33a2feb5e.html.gz`，`comretCount=1244`）：當時 `hasAvailableRoom=true` 363 棟、`availableRoomCount` 總和 = **802 間**，`totalRoomCount` 總和 15,696 間（含満室）。

**我們的量**

三層數字不同，必須分清楚：

1. **真相層 normalized**（`data/normalized/hituji.ndjson.gz`，2026-08-16 抓）：`gzcat | wc -l` = **1,244 棟**——站方當時的 1,244 棟**一棟不漏全部都在**。其中 358 棟有房間、886 棟房間數 0；房間合計 **545 間**（unit 數分布 {0:886, 1:181, 2:171, 3:2, 4:4}）。
2. **原始 HTML**：`data/raw/hituji/` 共 1,246 檔 = 1,244 詳情頁 + 2 列表頁（兩張列表頁的 `"comretCount":1244` 一致）。
3. **網站索引 index.json**（`web/public/data/index.json`，2026-08-23 產）：`b.src` 中 hituji（dict 索引 2）= **346 棟**；`u.bid` 指向這些棟的 unit = **516 間**。與交辦單上的 346/516 完全吻合。

358→346 / 545→516 的落差來自 build-data 的跨來源 alias 合併：`data/aliases/buildings.json` 共 139 組，其中 24 組的 primary 不是 hituji 而 members 含 `hituji:`（例：`{"key":"三鷹市|アーバンハウス吉祥寺","primary":"tokyosharehouse:1712","members":["hituji:mitaka/urban-house-kichijoji"]}`），這些 hituji 棟被折進 tokyosharehouse 的主列。

**成因**

**(A) 898 棟部分：legitimate（不是漏抓，且無法補）。** ひつじ不動産對満室物件不刊登任何房間與價格。證據：1,244 份詳情頁 HTML 中，358 份含房間資料、900 份含「満室」字樣、**0 份兩者皆無**（乾淨二分，沒有解析失敗的殘餘）。今日列表卡片上，853 棟満室物件的 `price` 欄位字面就是字串 `"満室"`（例 `["バウハウス 南千住","満室"]`），不是金額。既然站方不刊房價，這 898 棟本來就沒有可比價的資料可收。

**(B) 257 間部分：under-crawl，站方把詳情頁的房間陣列截斷成「每種房型最多 2 筆」的預覽。**

詳情頁 `/comret/info/tokyo/{ward}/{slug}` 的 RSC payload 只放 `singleRoom`／`dormitoryRoom` 兩個**預覽陣列**，各硬上限 2 筆。掃過全部 1,244 份存檔詳情頁：`maxSingle=2, maxDorm=2`，單／宿舍組合分布 `{"0/0":886,"1/0":180,"2/0":168,"2/2":4,"0/2":2,"0/1":1,"1/2":1,"1/1":1,"2/1":1}`——沒有任何一棟超過 2。

完整清單在另一個 URL：詳情頁 payload 自己就寫了 `"roomsUrl":"https://www.hituji.jp/comret/info/tokyo/bunkyo/hakusan-house/rooms"`（1,244 份詳情頁**全部**都有 `roomsUrl`）。該頁的 payload 多一個 `comretRooms` 物件，鍵名是**複數**：`comretRooms":{"singleRooms":[...],"dormitoryRooms":[...]}`，裝的是全部空房。

adapter 抓不到它的直接原因：`crawler/src/rsc.ts:98` 的 `extractArrayAfterKey` 用 `const needle = '"'+key+'":'` 做精確鍵比對，而 `crawler/sources/hituji/index.ts:166-167` 只餵單數鍵 `'singleRoom'` / `'dormitoryRoom'`，永遠只會撿到那個 2 筆的預覽陣列；同時 `index.ts:390` 的 `yield { url: s.webUrl, ... }` 只把詳情頁排進抓取，`/rooms` 從頭到尾沒被請求過。

這個漏抓不會報錯：`discover` 在 `index.ts:383` 有守門（解析數 < 站方數 95% 就 throw），但它守的是**棟**數，棟數一直是滿的，所以房間被砍掉一半也不會有任何警訊。

**修法**

**共同必改（不改這一條，抓了 /rooms 也還是只拿 2 筆）**

`crawler/sources/hituji/index.ts:163-176` `parseRooms()`：在現有兩行之前先試複數鍵，取到就用完整清單，取不到才退回預覽鍵。
```
現行 :166-167
  ...extractArrayAfterKey<HitujiRoom>(buf, 'singleRoom').map(r => ({...r, __kind:'個室'})),
  ...extractArrayAfterKey<HitujiRoom>(buf, 'dormitoryRoom').map(r => ({...r, __kind:'ドミトリー'})),
改成先讀 'singleRooms' / 'dormitoryRooms'（comretRooms 底下的完整陣列），
兩者皆為空時才退回 'singleRoom' / 'dormitoryRoom'。
```
複數鍵在詳情頁不存在、在 `/rooms` 頁一定存在，所以 fallback 讓兩種頁面都能正確解析，也不會誤把預覽和完整清單混疊（現有 `seen` 依 `r.id` 去重，混到也不會重複計）。

**接著要決定抓哪一頁——兩個方案，取捨不同，我不替你裁決：**

*方案 1（請求數不變，會掉一個欄位）*
`index.ts:390` 把 `yield { url: s.webUrl, ... }` 改成 `yield { url: \`${s.webUrl}/rooms\`, ... }`。
- 請求數與現在完全相同（每棟 1 次），對站方負載零增加。
- `buildBuilding` 用的是 `keysFromUrl(s.webUrl)`（hint 裡的原網址），不受影響；`parseTownName` 在 `/rooms` 頁實測仍回「文京区」。
- **代價**：`parseTenancyFromHtml`（`index.ts:234-245`）讀的「入居期間」在 `/rooms` 頁不存在（該頁 HTML 中「入居期間」出現 0 次），目前 545 間裡有 542 間填著這個值，會全部變成空。
- **補償**：`/rooms` 頁反而多了比現在更好的結構化欄位——`qualificationJapanese`/`qualificationForeigner`（原文：`"qualificationForeigner":"パスポート、学生証もしくは入学許可証。"`）可取代目前用 HTML 文字正規表示式硬撈的 `外国人：…`，另有 `transportations[]` 帶 `primaryTrainLineName`（現在 `stations[].line` 一直是空字串）與 `timeMinutes`。

*方案 2（零資料損失，請求數 +31%）*
詳情頁照舊抓（保住 入居期間），另外對 `hasAvailableRoom===true` 的棟（今日 387 棟）在 `discover` 內用 `fetcher.get(\`${s.webUrl}/rooms\`)` 補一次，把解出的完整房間陣列塞進 `hint`，`extract`（`index.ts:399`）優先用 hint 裡的房間、沒有才 `parseRooms(raw.body)`。
- 請求數 1,240 → 1,627（+387），以 3 秒間隔計約多 19 分鐘。
- `discover` 本來就拿得到 `fetcher`，介面不用動；`extract` 拿不到 fetcher，所以第二次請求只能放在 discover。

**順帶要一起修（否則下次 crawl 會靜默掉欄位，見 caveats）**：站方列表 summary 的 schema 已變，`HitujiSummary`（`index.ts:75-97`）宣告的 `totalRoomCount`、`availableRoomCount`、`nearestTrainStationName`、`transportationName`、`transportationTimeMinutes`、`hasAvailableRoomForForeigner`、`minRent`/`maxRent`、`ownerName` 今日在列表 payload 中一個都不存在。這些欄位改由詳情頁／`/rooms` 頁的 `transportations[]`、`singleRoomAvailability.totalCount`、`tenancyConditionPictgramCodes` 取得。

**驗收方式**：修完對上面第 5 點那 5 棟重跑 `parseRooms`，結果應為 6 / 11 / 8 / 12 / 0，與站方 `availableCount` 逐棟相等。

**預期增加**

以 2026-08-16 同一份快照（可精確驗算的唯一基準）計：

- **房間：545 → 802 間（+257，+47.2%）**。依據是站方自報 `availableRoomCount` 總和 802，且抽驗 5 棟顯示 `comretRooms` 完整陣列與 `availableCount` 逐棟相等，故 802 是修好後應拿到的數。
- **可比價的棟：358 → 363 棟（+5）**。有 5 棟站方說有空房但預覽陣列給 0 筆，目前完全沒有房間資料。
- **索引層（經 alias 合併後）：516 間 → 約 760 間**。此數為按 802/545 比例推估（0.947 的合併保留率 × 802 ≈ 760），**未實跑 build-data 驗證**，標記為推估。棟數 346 → 約 351。
- 今日（1,240 棟／387 棟有空房，比 8/16 的 363 棟多 6.6%）站方不再公開 `availableRoomCount`，今日的絕對值無法計算；不外推。

**拿不到的部分（修了也拿不到）**：853 棟満室物件的房間與價格，站方一律不刊（卡片 `price` 欄位就是字串「満室」）。

成本：方案 1 請求數不變；方案 2 每次 crawl 多 387 次請求、約多 19 分鐘。

**合規記錄**：**robots.txt**：本次實抓 `https://www.hituji.jp/robots.txt`（200，225 bytes），內容與 manifest 記錄的 2026-08-16 版**完全一致**，未變：
```
User-agent: *
Disallow: /owner-members/
User-agent: *
Disallow: /admin-members/
User-agent: *
Disallow: /comret/info/*/*/*/ask$
User-agent: *
Disallow: /sns/auth/
Sitemap: https://www.hituji.jp/sitemaps/sitemap_index.xml
```
無 `Crawl-delay`。用專案自己的 `parseRobots`+`isAllowed`（`crawler/src/robots.ts`）跑判定：
- `/comret/info/tokyo/bunkyo/hakusan-house/rooms` → `{"allowed":true,"reason":"無相符的 Disallow 規則"}`
- `/comret/info/tokyo?page=48` → `{"allowed":true,...}`
- 對照組 `/comret/info/tokyo/

**異常與不確定**：**1.（重要，非本次任務範圍但會咬人）站方列表 payload 的 schema 已經改了，下次 crawl 會靜默掉欄位。**
今日 page=1 與 page=48 的 summary 物件中，以下欄位出現次數皆為 **0**：`totalRoomCount`、`availableRoomCount`、`transportationTimeMinutes`、`hasAvailableRoomForForeigner`、`minRent`。新增了 `nearestTransposionLabel`（顯示字串，如「聖蹟桜ヶ丘駅 徒歩8分 」）與 `price`（如「￥47,000 - 48,000」，満室者字面為「満室」）。`hasAvailableRoom`、`tenancyConditionDescription`、`webUrl`、`name` 仍在。
影響：`stationsOf`（`index.ts:352-366`）會回空陣列 → 全站車站與步行分鐘全失；`buildBuilding` 的 `totalUnits` 變 notListed；`foreignerPolicy.welcomed` 變 notListed。這三項不會報錯。我沒有修改任何檔案，只是回報。

**2. 802 這個數字是 2026-08-16 的，不是今日的。** 站方今日已不公開 `availableRoomCount`，今日東京可入住房間總數我**取不到**，沒有猜一個填上。今日確定的只有：1,240 棟、387 棟有空房、853 棟満室。

**3. 「+257 間」的性質**：是「站方自報 802 − 我們實得 545」，屬同一份 8/16 快照的內部對帳，可完全復算；不是今日的缺口。今日缺口未量。

**4. 抽驗量**：`/rooms` 只實抓 5 棟（含 1 棟対照組）。5

### tokyosharehouse（東京シェアハウス, https://tokyosharehouse.com/ ） — **確認漏抓，可補**

**站方自報**

站方沒有公布「東京都 N 件」這個數字，所以分兩層說：

【全站總數（站方自報，2026-09-06 實測）】1,690 棟。取得方式：GET /jpn/area/ 回的分頁 `rel="last"` 指向 `/jpn/area/index/page:169`；再 GET page:169，`#listContentArea` 內恰好 10 個 `div.listItem`、且無 `rel="next"`；再 GET page:170 得 HTTP 404。每頁 10 筆 → 168×10+10 = 1,690。此清單橫跨全日本（page:169 的 10 筆地址含長野県／大阪府／大分県／和光市），不是只有關東。

【東京都部分】1,074 棟。取得方式：站方在每個列表項的「エリア」欄直接印出地址，我把手上 193 張 area 列表頁（2026-08-16 抓取）全部解析，得 1,687 個不重複物件 id，其中地址含「東京都」934 筆＋省略都名直接寫 23 区 140 筆 = 1,074 筆。這是站方頁面原文，但日期是 2026-08-16 而非今天。
【東京都房間數（站方自報）】13,977 間。取得方式：站方詳情頁「世帯数」欄，我們已解析進 building.totalUnits，1,068 棟全部有值，加總 13,977。

我試過用站方篩選表單直接要一個東京數字（GET /jpn/area/index/?search_type=filter&address=東京都），回 200 但沒有生效——第一筆結果仍是神奈川県川崎市，且頁面無 rel="last"。該 address 欄是地圖用的 geocode 輸入框，不是 server-side 縣市篩選，所以拿不到站方自報的東京計數。

**我們的量**

normalized 層：1,068 棟東京建物 / 898 間房（指令：解 data/normalized/tokyosharehouse.ndjson.gz，1068 行，units 加總 898；與 data/health/latest.md:415「建物 1068 棟 / 房間 898 間」一致）。
其中 507 棟有 ≥1 間房、561 棟 0 間房。
發布層（你表格裡的 469 / 833）：build-data 再砍掉 0 房建物（crawler/src/cli/build-data.ts:369）與跨來源 alias 併房（同檔 :322）後的結果。507→469、898→833 這段是既有閘門行為，不是抓取缺口。

**成因**

分三段，兩段正當、兩處是漏抓：

【正當 1｜52 個 area 與分頁沒有缺口】首頁今天列出的 area id 就是我們爬過的那 52 個（完全相同）；193 張列表頁裡每個 area 都從 page:1 走到它自己的 `rel="last"`，逐 area 檢查 missing 頁 = 全空。52 area 聯集 1,687 筆 vs 站方全站 1,690 筆 → discover 實質上把整站枚舉完了。**這一層沒有漏抓。**

【正當 2｜站方詳情頁本來就不給滿室房】1,686 張詳情頁的 `room-item` 狀態統計：available 1,430、coming_soon 239、occupied **0**；其中 808 張（東京 1,068 棟裡的 561 棟）連一個 room-item 都沒有 = 該棟現在滿室。所以「1,068 棟只有 507 棟產出房間」不是 extract 丟資料。

【漏抓 A｜〒郵遞區號開頭的東京地址被誤判成外縣市，6 棟被丟掉】parseTshAddress（index.ts:150-166）先 `startsWith('東京都')`（:154）判東京，失敗後在 :159 用 `/[都道府県]/` 判外縣市。「〒164-0003 東京都中野区東中野５丁目」不以「東京都」開頭，但含「都」字 → 回 kind:'other' → extract:539 `return null` 靜默丟棄。實測受害 6 棟：4239 / 4070 / 4320 / 4366 / 3107 / 4357。

【漏抓 B｜整個「全て表示」房間頁沒抓，滿室房完全沒收】站方另有 `/jpn/house/room/{id}/`（「全て表示」分頁），會列出**全部**房間含滿室。adapter 從沒請求過它——discover 只 yield 詳情頁（:483），extract 又只留 available/coming_soon（:554）。檔頭註解寫「不需要為了拿房間再多打一次 room 頁」，那個結論只在「不要滿室房」的前提下成立。這 13,977−898 ≈ 13,079 間房是站方公開的，我們一間都沒拿。（是否該收滿室房是產品決策：schema 有 isVacant，TSH 本身已存了 166 筆非空室的「空室予定」，所以模型支援；但收進來的價值取決於你要不要讓使用者看到滿室房的行情。）

**修法**

【修法 A（小、無爭議、必修）】crawler/sources/tokyosharehouse/index.ts:151
現在：`const t = raw.replace(/\s+/g, '').trim();`
改成先剝掉開頭郵遞區號再判斷，例如：`const t = raw.replace(/\s+/g, '').replace(/^〒?\d{3}[-‐−ー]?\d{4}/, '').trim();`
理由：:154 的 startsWith('東京都') 與 :159 的 /[都道府県]/ 兩條規則在「〒xxx-xxxx東京都…」上互相打架，後者先中。剝掉郵遞區號後兩條規則語意回到原設計，不需要動 :154/:159。
驗收：對上面 6 個實際字串跑 parseTshAddress 應全回 kind:'tokyo'、ward 分別為 中野区／杉並区／葛飾区／新宿区／新宿区／新宿区。

【修法 B（大、需你先裁決要不要收滿室房）】要拿 /jpn/house/room/{id}/ 的全部房間，架構上一個 RawDoc 只能出一個 Listing，所以走既有的 hint 通道：
1. discover（:466-486）在 yield 前，用 fetcher 多打一次 `${SITE}/jpn/house/room/${id}/`，把 body 放進 `TargetRef.hint`（types.ts:51 的 hint 就是為「列表階段先取得的資料」設計的）。
2. extract（:552）改為：hint 有 room 頁就用它當房間來源，沒有才退回詳情頁。
3. parseTshRooms（:266）錨點要放寬：room 頁用的是 `<div class="content-room">` 而非 `<div class="detail-room`，也沒有 `<a name="condition">` 結尾錨（實測 indexOf 皆為 -1）；其餘 room-item / room-num / btnOccupied / room-floor / gender-icon / `<span class="width">` / 賃料‧共益費 label 結構完全相同。
4. :554 的過濾要放行 occupied，並讓 isVacant=false；賃料為「-」時 moneyField 會走 unparsed 分支，要改成 notListed（"-" 是站方明示未公開，不是解析失敗），否則健康報告會噴假告警。
5. 成本：每棟多一次請求。若同時利用列表頁 listItem 內的「エリア」地址先濾掉非東京物件，可以只對東京的 1,074 棟打 room 頁，並順便省掉現在白抓又丟棄的 612 張外縣市詳情頁——請求數約 193+1,074×2 = 2,341，與現行 1,879 相比只多約 25%。

**預期增加**

修法 A：+6 棟東京建物、+7 間房（4070:2、4239:2、4366:1空室+2空室予定；4320／3107／4357 目前滿室、0 房）。過 build-data 的 0 房閘門後實際發布 +3 棟 / +7 間。這是拿 2026-08-16 快照實測的數，不是估計。

修法 B：房間數上限 13,977（站方 totalUnits 加總），即現行 898 的 15.6 倍，新增約 13,079 間；建物層可讓 561 棟目前 0 房的滿室建物變成有內容（發布層 469 → 最多 1,074 棟）。⚠️ 但「賃料可用」的比例我沒有量到：兩個樣本一個全有價（3397，5/5 有 ￥）、一個全無價（304，10 間 occupied 全是「-」），n=2 不足以推比例。若你要決策，建議先隨機抽 20 棟 room 頁量一次 occupied 房的賃料填充率再定。

**合規記錄**：robots.txt 今天重新抓過並全文比對：HTTP 200、84 bytes、三行「# updated 2026-02-12」「User-agent: *」「Sitemap: https://sharehouse.in/sitemap_index.xml」，零 Disallow、無 Crawl-delay，與 adapter manifest.legal.notes 記錄的 2026-08-16 內容完全一致。本次打到的所有路徑（/jpn/、/jpn/area/、/jpn/search/、/jpn/area/index/page:N、/jpn/house/detail/N/、/jpn/house/room/N/、/jpn/house/room/N/empty/）都沒有被 Disallow。

實際請求數 14 次（另有 1 次 /jpn/area/index/page:1 是否送出不確定，見 caveats），全部單執行緒、每次間隔 3.5 秒（腳本 setTimeout 3500ms）、User-Agent 用 crawler/src/http.ts:20 的 USER_AGENT 常數原文。無 CAPTCHA、無 challenge、無 403/429，唯一非 200 是我故意探底的 page:170 → 404。

沒有跑 crawl，沒有動專案任何檔案；所有抓下來的 H

**異常與不確定**：1. 站方沒有公布東京計數，1,074 這個數是我用站方列表頁上印的「エリア」地址自己數的，而且用的是 2026-08-16 抓下來的 193 張列表頁，不是今天的頁面。今天的全站總數 1,690 對上當時的 1,687，差 3 筆——可能是這三週新增，也可能是我沒有今天逐頁重數所致；我沒有為了精確而去打 169 頁（會超出你給的請求上限）。

2. 「13,977 間」是站方「世帯数」欄加總，我只用兩棟驗證過世帯数 = room 頁房間數（3397: 5=5、304: 12=12）。n=2。多人房／ドミトリー 的世帯数是算床位還是算房間，我沒有查證，所以這個數字對這類物件可能偏差。

3. 滿室房的「賃料可用率」完全沒有量測，只有兩個相反的樣本（3397 全有價、304 全「-」）。修法 B 的實際價值取決於這個比例，不要拿我的 13,079 當「可用房源數」。

4. 鮮度有個不能忽略的上界問題：1,068 棟裡有 643 棟（60.2%）站方根本沒印「データ更新日」。所以「超過一年」的真實比例下界是 25.6%（273/1068），上界是 85.8%（273+643），我無法把那 643 棟收窄。回報 25.6% 時必須連這句一起講，否則會低估。

5. 我們自己的資料是 2026-08-16 抓的，距今 21 天。實測已看到位移：Cosmos 世田谷（304）我們存的空室是 203/104/103，今天站上是 202/203 空室、103/104 已滿。所以「898 間」是三週前的狀態。

6. 「站方詳情頁 0 個 occupied」是對我們手上 1,686 張存檔頁面統計的結論，不是對今天全站重新驗證；我今天只驗了 3397 與 304 兩棟的 room 頁行為。

7. 修法 B 我沒有寫任何程式碼、沒有跑過，所以「架構上 hint 可行」是讀 types.t

### oakhouse（Oak House／オークハウス, https://www.oakhouse.jp/ ） — **確認漏抓，可補**

**站方自報**

取得方式：GET https://www.oakhouse.jp/area/tokyo （2026-09-06，HTTP 200，190,562 bytes），「カテゴリから探す」區塊是**伺服器端算好寫在 HTML 裡**的，原文如下（我從回應 HTML 逐字取出）：

- 東京都の**シェアハウス**（84物件, 1818部屋）
- 東京都の**アパートメント**（448物件, 1436部屋）
- 東京都の**家具家電付きアパートメント**（323物件, 1152部屋）← 是アパートメント的子集（篩選條件），不可另計
- 東京都の**ソーシャルレジデンス**（14物件, 887部屋）
- 東京都の**グランステイ**（2物件, 3部屋）

合計取決於「ソーシャルレジデンス 是否已含在 シェアハウス 84 之內」——頁面沒有寫，我無法從該頁判定，所以只能給區間：
- 若 SR 與シェアハウス互斥：**548 物件 / 4,144 部屋**
- 若 SR ⊆ シェアハウス：**534 物件 / 3,257 部屋**

另有一個數字我**刻意不採用**：同頁搜尋結果區寫「0 件の物件、0 部屋がヒットしました」——那是 JS 未執行時的初始佔位值，不是真實數量。

分頁數推算不適用：搜尋結果由 API 前端渲染，且 robots.txt 明文 Disallow `*p=`（分頁參數），我沒有去打分頁。

**我們的量**

我自己從 `data/normalized/oakhouse.ndjson.gz` 算出（gunzip 後逐行 JSON.parse）：

- **565 棟**（東京，非東京已被 adapter 排除）
- `building.totalUnits` 加總 = **2,365 間**（棟層記錄的房間數）
- 實際產出的 `units` = **99 間**（只有空室房才變成 unit）
- 拆線：apartment 線 470 棟 / totalUnits 394 / 空室 32；house 線 95 棟 / totalUnits 1,971 / 空室 67

索引裡的 51 棟 / 95 間：由 565/99 經 build-data 兩道處理後得出，算式完全對得起來（見 evidence）。

**成因**

`crawler/sources/oakhouse/index.ts:143` 的房間列舉正則只匹配表格列：

```
for (const m of html.matchAll(/<tr\s+id="(\d+)"[\s\S]*?<\/tr>/g)) {
```

Oak House 兩條產品線用**兩種不同的房間 DOM**：
- share house 線（多數 `/house/{n}`）：房間是 `<tr id="123" data-sort_price=… data-status=…>` 表格列 → 我們抓得到
- apartment 線（多數 `/apartment/{n}`）：房間是 `<article class="p-room__caset " data-sort_number="" data-sort_price="" data-status="vacancy" data-floor="4" data-type="apartment" …>` → **正則完全不命中，整棟回 0 間房**

因為房間數是 0，`totalUnits` 記 0、`units` 為空陣列，該棟接著在 `crawler/src/cli/build-data.ts:369`（`if (units.length === 0) { emptyBuildings += 1; continue; }`）被整棟丟掉。所以 451 棟東京物件在索引裡完全不存在——不是「沒有空房」，是**有空房但我們沒看到房間**。

附帶兩個同源的欄位漏失（同一個「兩種版型」問題）：
- `:158` 面積正則 `/広さ｜\s*([0-9.]+\s*(?:㎡|m2|m²))/`：article 版型寫成 `広さ：｜16.52m&sup2;`（多一個全形冒號、單位是 HTML 實體 `m&sup2;`）
- adminFee 走 `moneyOf(t, '共益費')`：article 版型標籤是 `共益費・管理費｜`，`共益費｜` 匹配不到（實測 `/house/5` 有 33 個 `共益費｜`、0 個 `共益費・管理費`；`/apartment/10210` 相反）

**修法**

**主修（拿回 449 棟 / 1,017 間，不需重抓）**

1. `crawler/sources/oakhouse/index.ts:143` — `parseRooms` 目前只跑一個迴圈。改成先跑現有的 `<tr id="N">` 迴圈，若為 0 筆再跑第二個迴圈掃 `/<article class="p-room__caset[\s\S]*?<\/article>/g`（實測兩種版型不會同時出現，`both: 0`，所以「二選一」是安全的；也可兩個都跑後合併）。
   article 版型的欄位對應（已逐項實證）：
   - 房號：`<p class="p-room__caset__number">402</p>`（`:150-152` 的房號正則要換，因為前後不是「｜満室/空室」而是「｜空室予定｜日期」）
   - `data-status` / `data-floor` / `data-type` / `data-gender` 屬性名與 tr 版**完全相同** → `:154-156`、`:169-176` 的 `attr()`、vacant 判定、kind、floor 全部可原樣沿用
   - 賃料：`賃料｜ ｜ ¥90,000` → 現有 `moneyOf(t,'賃料')` 直接可用（實測 1,017/1,017 命中）

2. `:158` 面積 — 正則改為容許全形冒號與 HTML 實體：
   `/広さ[：:]?｜\s*([0-9.]+)\s*(?:m&sup2;|㎡|m2|m²)/`（article 寫 `広さ：｜16.52m&sup2;`）

3. `:159` 間取り — 同樣容許 `：`：`/間取り[：:]?｜[\s｜]*([0-9A-Za-z]{1,6})/`

4. adminFee 的 `moneyOf(t, '共益費')` — 標籤放寬成 `共益費(?:・管理費)?`。article 版寫 `共益費・管理費｜ ¥11,000`，現在會整個漏掉管理費。

5. `:163` `入居条件` 的結束錨點（`｜内装`／`｜ 空室通知`）在 article 版不存在，需補 article 版的結束標記，否則性別／外国人可 判定會吃到過長文字。

**費用欄位的陷阱（我已排除，但實作時別踩）**：apartment 線的建物徽章是「敷金なし／礼金**あり**／保証金なし／保証人不要／保証会社**必要**」，`parseBadges` 用 `has('礼金なし')`／`has('保証会社不要')` 判定，在這些頁上正確回 false，`zeroIf` 因此不會寫出假的 0——線上 `/apartment/10210` 實測確認。**不可**為了「補齊」而把建物徽章套到 article 房間上：這些房間有逐間的 礼金1ヶ月／仲介手数料 賃料の1.1ヶ月分／事務手数料 22,000円／鍵交換費用 33,000円／室内清掃費用 55,000円／初期費用概算 ¥506,500，與 share house 線的「全零」是完全不同的收費結構。

**執行方式**：不需要重抓。raw HTML 已在 `data/raw/oakhouse/`（994 檔），跑 `npm run reparse`（`crawler/src/cli/crawl.ts --offline`）即可。⚠️ 但要預期：新增 1,017 間會讓**閘門 4** 冒出大量「跨來源同房未審核」錯誤（build-data:306-311 會直接不產檔），需要先跑 `npm run alias:candidates` 並人審。這件事我沒有做，也沒有動任何檔案。

**次要修（滿室房納入，子問題 3）**
`:336` 的 `rooms.filter((r) => r.vacant)` 若要放寬，**必須同時修 `:375`**——`isVacant: known(true, 'measured', …)` 是寫死的 `true`，只因為目前只有空室房走到這裡才碰巧正確；一旦放寬過濾條件，2,266 間滿室房會全被標成「空室」，那會直接污染空房篩選與行情統計。正確寫法是 `known(r.vacant, 'measured', …)`。

**預期增加**

**主修（article 版型）**：+449 棟 / +1,017 間空室房（東京）。索引口徑 51 棟 → 約 500 棟、95 間 → 約 1,112 間（房間數約 11.7 倍）。品質不打折：1,017 間全部有可解析租金與面積，1,016 間有間取り、1,004 間有樓層，租金中位數 ¥145,000（¥58,000–¥830,000），涵蓋 34 個区市。這批全是**現在可申請的空室**，不是滿室資料。

以站方自報數字對照：站方東京アパートメント 448 物件，我們回收的正好是 448 棟 apartment 線——等於把整條 apartment 產品線從「幾乎全空」補到接近站方公布的規模。

**次要修（納入滿室房）**：另外 +2,266 間（share house 線的 `novacancy` 房）。這批**100% 有公布賃料、99.96% 有面積**（2,266/2,266、2,265/2,266），所以對「讓出租方看到完整競品行情」確實有實質價值，不是空殼資料。
成本與風險：(a) 必須先修 `:375` 的 isVacant 寫死 true，否則滿室房會被當成空房；(b) 這 2,266 間不可進入空房篩選，且必須排除在「行情（僅計月額完整可比者）」統計之外，否則中位數會被滿室房拉動；(c) 索引體積會再漲——首屏索引已是 1,564 KB gzip、超出 500 KB 預算 3 倍，再加 2,266 間需要重新評估。
我的建議：**主修先做（純獲利、無口徑爭議），滿室房另案**，因為它牽涉排序／統計口徑與索引預算，屬於需要使用者裁決的取捨，不是單純的漏抓修補。

**合規記錄**：- **先讀 robots.txt**：本次第一個請求就是 https://www.oakhouse.jp/robots.txt（2026-09-06，HTTP 200，111 bytes）。全文只有 `User-agent: *`、`Sitemap: https://www.oakhouse.jp/sitemap.xml`、`Disallow: *p=`、`Disallow: *?fid=`、`Disallow: */booster/`。與 adapter `manifest.legal` 記錄的 2026-08-16 內容一致，未變更。
- **我打的每個 URL 都逐一比對過三條 Disallow，全部不含 `p=`、`?fid=`、`/booster/`**。沒有碰任何分頁參數 URL。
- **請求總數 10 個**（在「最多十幾個頁面」內）：robots.txt、sitemap.xml、sitemap-pages.xml、area/tokyo、video-sitemap.xml、house/1117、apartment/1139、house/925、house_index、apartment/10210。全部 HTTP 200。
- **速率**：單執行緒、序列，每個請求前 `sleep 4`（>3 秒下限）。無平行請求。
- **User-Agent**：使用專案常數

**異常與不確定**：1. **sitemap 漏掉活著的物件頁，但缺口大小我量不出來。** 我證明了 `/house/1117`（オークアパートメント新宿，東京新宿区，14 間房）與 `/house/925`（ヘーベルSTAY代官山，東京渋谷区，24 間房）今天 HTTP 200 且是完整物件頁，卻同時不在 sitemap-pages.xml 也不在站方自己的 /house_index。要量這個隱藏集合只能暴力掃 ID 空間（現用 ID 從 5 到 18,293、密度僅約 5%），那是好幾千個請求，違反請求上限也違反本任務目的，**我沒有做，也不建議做**。所以這個子缺口只能記為「存在、未量化」。

2. **站方總數是區間不是定值。** /area/tokyo 沒有寫「ソーシャルレジデンス（14物件/887部屋）」是否已包含在「シェアハウス（84物件/1818部屋）」內，所以東京總數只能給 534–548 物件 / 3,257–4,144 部屋。我沒有用任何方式去猜哪一種。

3. **同頁的「0 件の物件、0 部屋がヒットしました」我沒有採用**——那是 JS 未執行的初始佔位值，不是真實數量。

4. **快取 603 頁 vs normalized 565 棟，差 38 頁我沒有追。** 我掃快取時用的住所正則比 adapter 略寬，且 adapter 另外要求 `buildingName()` 非空。這 38 頁的差異不影響回收量的計算（1,017 間是我在 article 房間上另外套空室條件算出來的，與這 38 頁無關），但它本身是一個未查明的小差距。

5. **`/house/` 與 `/apartment/` 路徑不能可靠代表產品線。** `/house/1117` 是「オークアパートメント新宿」、`/house/925`（ヘーベルSTAY代官山）頁面掛的是 APARTM

### 新來源盤點（非既有 12 來源的覆蓋缺口）：實測 18 個候選站的 robots.txt 與實際房源頁 — **確認漏抓，可補**

**站方自報**

各候選站方自報東京數（全部為 2026-09-06 我實際抓頁面讀到的字面數字）：

【可抓・大型】
1. at-home（アットホーム）433,035 件 — https://www.athome.co.jp/chintai/tokyo/list/ 頁面文字「【エリア/路線・駅】東京都 【契約条件】定期借家含む 433,035 件」；同頁分頁連結最大 /chintai/tokyo/list/page3082/，每頁「1～30棟を表示」→ 約 92,460 棟。
2. CHINTAI 324,177 件 — https://www.chintai.net/tokyo/area/ 列出 49 個市區町村各自件數，我逐一相加得 324,177（千代田区5,088／中央区15,233／新宿区14,780／台東区20,872／墨田区23,110／江東区35,282…）。另單獨抓 https://www.chintai.net/tokyo/area/13104/list/ 顯示「新宿区の賃貸マンション・アパートの検索結果です。14868件」，與區頁 14,780 相近（即時波動）。
3. ハウスコム（housecom）188,853 件 / 建物 20,893 棟 — https://www.housecom.jp/tokyo/ 頁面文字「東京都の賃貸物件[マンション・アパート・戸建]188,853件、建物20,893件掲載中。物件更新日：2026年09月05日は6,362件更新。」
4. いい部屋ネット（eheya.net／大東建託）120,090 件 — https://www.eheya.net/tokyo/ 「掲載物件数 120,090 件」；為確認這是東京不是全國，另抓 https://www.eheya.net/ 得「全国の賃貸アパート・マンション物件情報…掲載物件数 746,403 件」，兩數不同 → 120,090 確為東京。
5. スマイティ（sumaity）242,155 件 — https://sumaity.com/chintai/tokyo/ 「242,155 件」。
6. エイブル（able）17,170 件 — https://www.able.co.jp/tokyo/area/ 列 49 市區町村件數，相加得 17,170（千代田区124／中央区426／新宿区771／江東区901…）；單抓 /tokyo/area/13104/list/ 顯示「該当物件 771 件」，與區頁一致。

【可抓・小而差異化】
7. goodroom 8,947 件 — https://www.goodrooms.jp/tokyo/ 搜尋篩選器內六個東京區塊數相加：都心1,806＋城東1,911＋城西1,007＋城南2,266＋城北824＋市部その他1,133。
8. シェアシェア（share-share）2,792 件 — https://share-share.jp/search/result/?pref=13 結果頁「東京都 のシェアハウスを探す --> 2792 件中 1～25件表示」。
9. XROSS HOUSE（x-house.co.jp）東京 1,023 棟 — https://x-house.co.jp/sitemap.xml 共 5,685 個 <loc>，其中符合 /sharehouse/{pref}/city/{city}/xross-N/ 的物件詳情頁 2,409 個，含 /tokyo/ 者 1,023 個（我用 python 對 sitemap 逐筆計數）。

【取不到數】
- ホームメイト（homemate）：https://www.homemate.co.jp/pr-tokyo/ 全頁無任何「N件」字串（grep 無命中），取不到東京數。
- Tokyo Room Finder：/property-search 回的頁 <title> 是「Terms and Conditions」、無任何物件連結，取不到數。

**我們的量**

目前 12 個來源合計（依你給的 2026-09-06 索引表相加）：棟 30,863 / 間 70,631。

我另外自己實跑 normalized 檔核對（指令：對 /Users/shrape/Desktop/Codex/東京租屋查詢/data/normalized/*.ndjson.gz 逐行 json.loads、以 units 陣列長度算間數）得：棟 32,992 / 間 74,967，其中 suumo 28,149 棟 / 69,608 間。兩者不一致（hituji 檔內 1,244 行 vs 表列 346、oakhouse 565 vs 51、tokyosharehouse 1,068 vs 469），推測索引表已過濾非東京或非現行列——我沒去查證這個差異的成因，基線以你給的表為準。

另實測 suumo normalized 的區分布：品川区3,686／江東区3,544／渋谷区3,307／豊島区3,293／目黒区3,005／台東区2,867／文京区2,581／中央区1,951，**新宿区為 0**，與「僅 10 区」相符。

**成因**

缺來源，不是既有來源漏抓。東京租屋市場的主要物件池有相當比例不在目前 12 個來源裡：

(a) **大型入口只收了 SUUMO 一家**。at-home 433,035 件、CHINTAI 324,177 件、housecom 188,853 件、いい部屋ネット 120,090 件全部 robots 允許、全部 SSR、全部有可預測的路徑分頁或 sitemap，目前一個都沒收。這四家與 SUUMO 有大量重疊，但各家都有只上自家平台的仲介店舖物件。

(b) **自社管理型業者只收了 leopalace21**。いい部屋ネット（大東建託 DK SELECT）、ホームメイト（東建コーポレーション）是同一類自社物件池，目前沒收。

(c) **share house 池明顯不足**。目前 share house 線最大的 tokyosharehouse 才 469 棟 / 833 間；單是 XROSS HOUSE 一家東京就有 1,023 棟可從 sitemap 直接列舉，シェアシェア 東京 2,792 件。

(d) **費用欄位的來源不足**。健康報告顯示仲介手数料／火災保險／鍵交換／保證公司初回 在多數來源是 `notOffered`（如 borderless 這些欄位全部 0 取得、56 筆「來源不提供」）。goodroom、CHINTAI、housecom 的詳情頁**明文刊登**這些數字，收進來可直接補上長期空白的欄位。

【必須排除、不是漏抓而是屏障】
- HOME'S（LIFULL）：robots.txt 讀得到且用專案 parser 判定 /chintai/tokyo/list/ 為 ALLOW，但實際 GET 回 **HTTP 403 CloudFront「Request blocked」**。是邊緣 WAF 不是 robots，屬屏障，不闖。
- アパマンショップ：robots 對 `User-agent: *` 是 `Disallow: /`（只白名單 Googlebot/GPTBot/ClaudeBot 等具名 bot，我們的 UA 不在內）。專案 parser 判定 BLOCK。
- GaijinPot Housing、Real Estate Japan：**robots.txt 本身**就回 403 Akamai「Access Denied」（edgesuite.net 參照碼），規則都讀不到，視同封鎖。
- ミニミニ：robots 允許該路徑（parser 判 ALLOW），但實際抓 https://minimini.jp/list/pref/tokyo/shinjukuku/ 回的頁 `<title>` 是 **「Checking your browser - reCAPTCHA」**。人機驗證，我打完這一次就停手，沒有重試、沒有換 UA。

**修法**

建議依此順序新增 adapter（每個都照現有 crawler/sources/{id}/index.ts 的 manifest＋legal＋discover＋extract 骨架，參考最接近的既有實作）：

**先做（差異化最高、成本最低、屏障風險最低）**
1. `xrosshouse` — 參考 crawler/sources/oakhouse/index.ts（同為 share house 營運商）。
   discover：GET https://x-house.co.jp/sitemap.xml 一次拿全部 2,409 筆，正則 `/sharehouse/tokyo/city/[^/]+/xross-\d+/$` 過濾出 1,023 筆東京。
   **關鍵限制**：manifest.legal.notes 必須寫明「robots 明文 Disallow `/*?page=` 與 `/ajax/load_more_properties/`，因此 discover 一律走 sitemap，禁止用分頁或 AJAX」——這點若搞錯會直接違規。
   extract：SSR HTML，可取 賃料／共益費／あんしんサポート料／初期費用／敷金0・礼金0・仲介手数料0／定期借家1年・最短1か月／面積／定員數／在留カード與對應言語。foreignerWelcomed、需在留卡、需日語能力這幾個現有多數來源填不出來的欄位，這家有原文可解析。
   crawlDelayMs: 3000。預期取得 1,023 棟。

2. `goodroom` — 參考 crawler/sources/couverture/index.ts（同為小型 SSR 站）。
   discover：/tokyo/ 頁的區塊篩選器已列出 6 區塊 + 49 市區町村與各自件數，可據以組出區級列表頁再分頁；詳情 /tokyo/detail/001/{id}/。
   價值不在量（8,947）而在欄位：這是我實測到**唯一同時明文刊登 仲介手数料（1.1ヶ月）、鍵交換費用目安、火災保険目安、保証会社料率、初期費用合計** 的站。健康報告裡這幾欄在 borderless 等來源是全 0 的 `notOffered`，收進來可直接讓比價的「初期費用」欄從空白變有值。

3. `sharesha`（シェアシェア） — 參考 crawler/sources/tokyosharehouse/index.ts（同為 share house 聚合站）。
   discover：/search/result/?pref=13（東京 2,792 件）分頁，或 /search/result/?city={jis5碼} 逐區；詳情 /house/detail/{id}。robots 對這些路徑無規則。
   extract：房型別（個室／ドミトリー）賃料與共益費、保証金、初期費用、性別／年齢／**国籍（日本国籍／外国籍）**入居條件。
   ⚠️ 這是聚合站，與 hituji／tokyosharehouse／oakhouse 重疊機率高——必須走專案既有的跨來源 alias 審核閘門（見 commit 464c6d0「跨站 alias 補審」、d529148「閘門 4 端到端測試」），不可繞過。

**再做（大型入口，需要先決定範圍）**
4. `housecom` — 四家大型入口裡我最推薦先做的：量夠大（188,853 件 / 20,893 棟）、URL 最乾淨（/room_{id}/、/build_{id}/）、robots 只有 7 條且都不擋房源、且詳情頁的費用欄位最完整（火災保險、保證公司、更新料、鍵交換、室內清掃、消毒施工料、フリーレント、他の空き部屋 一次到位）。棟/間關係站方自己分開標，正好對上專案的 Building/Unit 模型。
5. `chintai` — 324,177 件，初期費用拆項最細（鍵交換代／消火剤代／入居者サポート費用 各自獨立金額），robots 公告 sitemap_detail.xml 可直接枚舉。
6. `athome` — 量最大（433,035 件 / ≈92,460 棟），路徑分頁 /chintai/tokyo/list/page{N}/ 最單純。但 robots 完全沒有 `*` 群組＝零規則，**上線前務必先做 ToS 審查並在 manifest.legal 記錄**，不要把「parser 判 ALLOW」當成可以抓。
7. `eheya`（いい部屋ネット） — 120,090 件，補的是「大東建託自社管理池」，與現有 leopalace21 同類；Next.js SSR，/detail/{21碼}/。

**規模化前的必要動作（照 dispatch.md §4）**：Tier 1 每家 3,000–92,000 個請求，以 3 秒間隔計是數小時到數十小時。動工前一定要先抓 10 筆詳情頁親自逐欄比對原頁，證明 extract 準確，再放大；且要先決定範圍（例如先只收 23 区、或只收有空室的），不要一開始就全量。

**明確不要做**：HOME'S、アパマンショップ、GaijinPot Housing、Real Estate Japan、ミニミニ——四家有屏障、一家 robots 全站禁止，全部維持現狀不闖。

**預期增加**

分階段（皆為站方刊登量，重疊未扣，見 caveats）：

第一階段（xrosshouse＋goodroom＋sharesha，三個小 adapter，估計各數百至數千請求）：
+1,023 棟 / +11,739 件（8,947＋2,792）。share house 棟數從 956 → 1,979（翻倍）；且首次讓「仲介手数料／火災保険／鍵交換／保証会社料率／初期費用合計／国籍條件」這些欄位有實際來源，不再全欄 notOffered。

第二階段（housecom）：+20,893 棟 / +188,853 件。棟數從 30,863 → 約 51,756（+68%）。

第三階段（chintai＋athome＋eheya）：+877,302 件、其中 at-home 一家約 +92,460 棟。合計東京刊登量會從 70,631 件量級進到百萬件量級。

⚠️ 三個階段的數字都會被重疊大幅稀釋，第二、三階段尤甚（同一批仲介店舖物件會同時上 SUUMO／at-home／CHINTAI）。真正的淨增只有做完跨來源 alias 比對才會知道，我沒有做這件事，所以以上任何一個數字都不能當成「會多拿到這麼多筆」來承諾。

**合規記錄**：- 全程用專案 UA（`TokyoRentCompare/0.1 (personal rental price-comparison aggregator; every listing links back to the source site; contact via repository issues) Node/25.9.0`，node -v 實測 v25.9.0 代入 crawler/src/http.ts 的模板字串）。
- 共 **63 次請求**，全部由 /private/tmp/.../scratchpad/fetch.sh 單執行緒串列送出，每次之間 `sleep 3`（≥3 秒），無任何平行請求。跨 18 個網域，單一網域最多 7 次（x-house），主要候選各 3–5 次。
- **每個候選都先抓 robots.txt 再抓頁面**，且不是目測判斷——我把抓到的 robots.txt 餵給專案自己的 parseRobots/isAllowed 逐條驗證後才發請求（見 evidence 的 check.ts 輸出）。
- 遇到防護一律停手：ミニミニ 回 reCAPTCHA 頁後**不再對該站發任何請求**、未換 UA、未嘗試繞過；HOME'S 回 CloudFront 403 後停止；GaijinPot／Real Estate Japan 連 robots.t

**異常與不確定**：- **CHINTAI 324,177 與エイブル 17,170 是我把區頁上 49 個市區町村數字相加得到的，不是站方直接印出的單一總數**。東京有 62 個市町村（23区＋26市＋5町＋8村），該頁只列 49 個，所以這兩個數是**下限**。加總指令與明細留在 scratchpad。
- **goodroom 8,947 同樣是我把 6 個區塊數相加**（都心／城東／城西／城南／城北／市部その他），非站方單一自報數。
- **單位不一致，不可直接互相比較**：at-home 433,035「件」但分頁是「棟」（3,082 頁×30 棟≈92,460 棟）；housecom 明確分開 188,853 件 vs 20,893 棟；シェアシェア 2,792「件」我無法確定是「棟」還是「房間」——同站首頁行銷文案寫「日本最大級2500件以上」（全國），與東京 2,792 相矛盾，其中至少一個不是同一種單位。我沒有為了釐清而多打請求。
- **x-house 1,023 是 sitemap 裡的物件頁數（棟），不是房間數**；每棟含多間，實際房間數會更多，但我沒抓任何一頁去數，不做推估。
- **重疊率完全未量化**。at-home／CHINTAI／housecom／sumaity 與現有 suumo 的重疊、以及 share-share 與 hituji／tokyosharehouse／oakhouse 的重疊，我一筆都沒比對過。上面所有數字是「站方刊登量」，不是「新增淨量」。任何「能多拿 N 筆」的說法都必須先做重疊抽樣。
- **スマイティ的物件來源未查證**。我在它的東京頁上 grep「提供／情報提供／LIFULL／アットホーム／出典／カカクコム」全部無命中，所以**無法判斷它是自有物件池還是轉載其他入口**，重複風險不明——因此我把它排在第 9 位而不是照 242,15

### sakurahouse — 正當差異（不是漏抓）

**站方自報**

**東京棟數：92 棟（站方自報 95 棟全站，扣掉 3 棟京都）。東京房間數：922 間（站方 payload 自算）。**

取得方式（全部離線，從 2026-08-16 抓取當下存下的清單頁快照 `crawler/sources/sakurahouse/fixtures/building-list.html.gz`）：

1. **站方自報全站棟數 = 95**。清單頁 `/building/` 的結果區標題逐字寫著：
   `<section class="buildResult"> <h2 class="buildResult__heading"> <span id="building_number">95</span> BUILDINGS </h2>`
   這是未套用任何篩選（表單 checkbox 全空）的預設清單計數。同頁 DOM 裡 `class="buildList__list"` 出現 95 次、`buildList__name` 95 次、唯一 `/building/{slug}` href 也正好 95 個——三個獨立計數互相一致，證明當次擷取到的是完整清單、沒有被 lazy-load 截斷。

2. **扣除京都得東京 92 棟**。95 個 slug 與我們 normalized 的 92 個 sourceKey 做 diff，差集正好 3 個且全是京都：`kyoto-annex`、`kyoto-imadegawa`、`kyoto-imadegawa-l`（指令：`comm -23 slugs95.txt got92.txt`）。反向差集為空（沒有我們有、清單沒有的）。

3. **站方東京房間總數 = 922 間**。adapter `index.ts:600` 把每棟 GraphQL payload 的房間數原封不動存進 `building.totalUnits`（`known(roomCount,'measured',\`payload 房間數 ${roomCount}\`)`），這個數字是**未經空室過濾**的全部房間。對 92 棟加總：922。分類別：sharehouse 671、apartment 249、dormitory 2。

4. **站方行銷口徑交叉驗證**：同一份清單頁的 `<p class="lead__text">` 與 meta description 逐字寫著「SAKURA HOUSE provides APARTMENTS, SHARE HOUSES, GUEST HOUSES & VACATION HOUSES -- over 1,000 rooms in 100 Tokyo & Kyoto locations」。這個「1,000 rooms / 100 locations」是**東京＋京都合計、且不分空室滿室**的整數行銷值。我們算出的東京 922 間 + 京都 3 棟，與之吻合，反證 922 這個數字沒有系統性短少。

**我們的量**

**92 棟 / 666 間**（我自己算的，非沿用交辦單）。

指令：`gzcat data/normalized/sakurahouse.ndjson.gz | wc -l` → 92 行（一行一棟）；逐行加總 `units` 陣列長度 → 666。

⚠️ 交辦單寫「90 棟 / 666 間」，**棟數與檔案實際內容不符**：檔案是 92 棟。666 間相符。`data/crawl-sakura.log` 也記錄「✔ 92 棟 / 666 間房」，與檔案一致，所以應是索引表那邊的 90 為舊值或筆誤。

区別分布（20 個行政區）：渋谷区 18、新宿区 15、江東区 10、豊島区 7、世田谷区 7、中野区 6、目黒区 5、北区 4、杉並区 4、板橋区 2、品川区 2、練馬区 2、中央区 2、国分寺市 2、台東区 1、千代田区 1、文京区 1、大田区 1、港区 1、調布市 1。

**成因**

**棟層級：零缺口（92/92，100%）。房間層級的 922→666 差距（256 間）全部來自「只收畫面顯示 AVAILABLE 的房間」這條刻意設計，不是漏抓。**

三個差距來源，全部正當：

1. **京都 3 棟被排除**——`index.ts:569` 的 `if (addr.prefecture !== '東京都') return null;`。專案只收東京，站方同時經營京都府與神奈川県。這是交辦單自己定義的正當原因（「我們只收東京」）。

2. **256 間房是「已滿室」**——`index.ts:621`：
   `if (r.occupiedLabel !== '' || r.availableLabel === '') continue;`
   只保留畫面標籤為 AVAILABLE 的標的，OCCUPIED 一律不收。這正是交辦單列的正當原因「站方數字含已滿室」。site 的 922（與行銷的 1,000）是總房間數，我們的 666 是**當下空房數**，兩者本來就不該相等。實測空室率 666/922 = 72.2%。

3. **枚舉本身沒有缺口**。LIST_READY 只要求 `>20` 個連結就收，理論上有截斷風險，但當次擷取到的 DOM 內 `#building_number` 自報 95、實際 render 出 95 張卡、95 個唯一 slug——三者一致，證明那一次拿到的是完整清單。crawl log 也記「列舉到 95 筆……抓取進度 95/95 (100%)　收錄 92　跳過 3　錯誤 0」，零錯誤、零逾時。

**不是 blocked-cannot-fix**：Cloudflare 防護確實存在，但它並沒有造成覆蓋缺口——有頭瀏覽器路徑把 95/95 棟全部枚舉到了。防護造成的是「每次更新都需要人在場」，不是「拿不到資料」。

**修法**

**不適用**（判定為 legitimate-difference，沒有需要修的漏抓）。

以下兩項是可選項，不是缺口修補，請使用者裁決：

**(A) 若要把「已滿室」也收進來（零額外請求，+256 間）**
那 256 間的資料**已經在我們抓下來的 GraphQL payload 裡**，只是被 `index.ts:621` 丟掉。schema 也早就支援表達滿室——`Unit.isVacant` 是欄位，只是 `index.ts:536` 寫死 `known(true, ...)`（實測 normalized 裡 666 筆 isVacant 全為 true）。若要收，改法是把 621 行的 `continue` 換成「照收但標記」，並讓 536 行依 `r.occupiedLabel !== ''` 決定 true/false。
⚠️ 但這需要使用者先裁決產品口徑：本站其他來源（share house 線）目前一律只列可租標的，把滿室房混進比價清單會改變「間數」在全站的意義，也可能誤導使用者。**我不自行認定該做**——這是產品決策不是 bug。

**(B) 枚舉完整性的防呆（不增加任何資料，只防未來靜默截斷）**
`index.ts:635` 的 `LIST_READY` 只要求 `>20` 個連結。清單頁自己印了 `<span id="building_number">95</span>`，可以拿它當斷言：ready 條件改成「slug 數 === #building_number 的值」，或在 `parseSlugs` 之後比對兩者、不一致就 throw（比照該檔既有的「大聲失敗」風格）。目前沒有發生截斷（95=95=95 三方一致），這純粹是把「這次剛好對」變成「以後對不上會被抓到」。

**預期增加**

**補齊覆蓋缺口的預期收益：0 棟 / 0 間。** 東京 92 棟已 100% 枚舉，沒有可補的東西。

若採用可選項 (A)（收錄滿室房，需使用者先裁決口徑）：東京由 666 → 約 922 間（+256，+38%），棟數不變仍 92 棟，且**不需要對站方發出任何新請求**（資料已在既有 payload 內）。此數字為 2026-08-16 快照的口徑；實際重跑時會隨當下空室狀況浮動。

若採用可選項 (B)：資料量 0 增益，只降低未來靜默漏抓的風險。

**真正會變動數字的是「重抓」而非「修 adapter」**：目前資料是 2026-08-16 的快照，距今 21 天，空室狀況早已變動。重抓需要使用者本人在場（見 caveats）。

**合規記錄**：**對 www.sakura-house.com 發出的請求數：0（零）。全程離線分析。**

- 依交辦單的絕對禁令，完全沒有觸碰該站：沒有 curl、沒有 fetch、沒有 WebFetch、沒有開瀏覽器、沒有讀 robots.txt、沒有碰 sitemap.xml、沒有碰 GraphQL 端點。
- 所有站方數字都來自 **2026-08-16 那次合規抓取當時存下的本機快照**：`crawler/sources/sakurahouse/fixtures/building-list.html.gz`（渲染後清單頁）與 6 份 `building-*.json.gz`（頁面自己載入的 GraphQL 回應原文）。
- 未遇到任何 CAPTCHA／challenge——因為根本沒連線。既有 adapter 的合規狀態（`manifest.legal`，`index.ts:96-130`）記載：robots.txt 於 2026-08-16 實測為 `User-Agent: *` / `Disallow:`（空值＝全站允許）/ Sitemap 一行，無 Crawl-delay，專案自訂 5000ms 間隔單執行緒；Cloudflare 僅以真實有頭瀏覽器正常載入通過，未做任何繞過；ToS 著作權與連結條的衝突已由使用者於 2026-08-16 閱讀後裁決自行承擔。以上為既有紀錄的轉

**異常與不確定**：1. **所有「站方數字」都是 2026-08-16 的快照，不是今天（2026-09-06）的**。距今 21 天。站方今天實際有幾棟、幾間空房，我無從得知也不去猜。「95 BUILDINGS」是 2026-08-16 當下清單頁自報的值。

2. **交辦單的「90 棟」與檔案不符**：`data/normalized/sakurahouse.ndjson.gz` 實際 92 行，crawl log 也寫 92 棟。間數 666 相符。索引表的 90 需要更正。

3. **922 vs 666 不是嚴格同口徑**：`totalUnits` 數的是「房間」，而我們收的 `units` 對 dormitory 是「一張床一筆」。實測 666 筆裡有 88 筆是床層級（含 `BED ` 標籤）。例：`tabata-3-dorm` payload 只有 2 個房間，卻產出 8 筆可租床位。所以真正的房間層級空室差距**略大於** 256，我沒有精算到床/房換算後的單一數字，因為那需要逐棟展開 payload 而我手上只有 6 份 fixture。

4. **「256 間全是 OCCUPIED」的歸因，證據強度分兩層**：922−666=256 這個減法是**全量**算出來的（92 棟全部）；但「這 256 全是 occupied 而非 isPublic=false 或標籤兩空」是**在 6 棟 fixture（33 房、49 個可租標的）上驗證**的，該樣本 18/18 排除皆為 occupied、另兩種成因各 0 筆。我沒有全量 payload 可重跑（raw 沒留，見第 5 點），所以第二層是樣本推論，不是全量證明。

5. **本來源沒有 raw HTML 目錄**：`data/raw/sakurahouse/` 不存在。這不是異常——manifest 是 `tran

### ur（UR賃貸住宅 / 都市再生機構） — 正當差異（不是漏抓）

**站方自報**

**308 是「建物數」，不是房間數——而且是東京全部 danchi 目錄數（含目前 0 空室者），不是空室物件數。**

取得方式（2026-09-06 實測）：
1. `POST https://chintai.r6.ur-net.go.jp/chintai/api/bukken/search/list_init/` body `tdfk=13`，逐字回傳：
   `[{"Key":"01","Value":20},{"Key":"02","Value":86},{"Key":"03","Value":36},{"Key":"04","Value":17},{"Key":"05","Value":32},{"Key":"06","Value":117}]`
   6 個 area，Value 合計 20+86+36+17+32+117 = **308**。
2. 逐 area 打 `list_bukken`（`tdfk=13&area=NN&vacancy=1`），回傳的陣列長度**逐區完全等於** list_init 的 Value：20/86/36/17/32/117，合計 308 筆建物物件、308 個 unique id。
   → 證明 list_init 的 Value = 該區建物數，308 = 建物數。

站方同一組 API 的「實際空室」數（2026-09-06）：
- 308 棟中 **255 棟 `roomCount` = 0**、只有 **53 棟有空室**；
- 全東京 `roomCount` 加總 = **119 間**。
（roomCount 分布：0→255 棟, 1→29, 2→15, 3→3, 5→2, 6→1, 7→1, 13→1, 15→1。）

站方 HTML 頁面（`https://www.ur-net.go.jp/chintai/kanto/tokyo/`, HTTP 200, 288,908 bytes）本身**沒有**印出任何「N 件／N 棟」數字，物件數是前端用上述同一組 API 算的，所以 308 就是站方自報數字。

**我們的量**

**69 棟 / 150 間**（2026-08-16 抓取快照）。

自算指令與輸出：
`gunzip -c data/normalized/ur.ndjson.gz | wc -l` → `69`
逐行 JSON 加總 units → `buildings 69 units 150`
與 `data/health/latest.md:460`「建物 69 棟 / 房間 150 間」一致。

**69 棟全部都在站方今天的 308 id 清單內**（交集 69，差集 0），沒有殘留已下架的假物件。

**成因**

「308 vs 69」是**拿兩種不同東西相減**造成的誤會，不是漏抓：

1. **308 = 東京全部 UR danchi 的目錄數，含目前完全滿室的 255 棟。** `vacancy` 參數實測**完全無作用**：area=01 分別打 `vacancy=1`、不帶 vacancy、`vacancy=0` 三次，都回傳同樣 20 棟、roomCount 合計都是 0。所以 list_bukken 從來就不是「只回空室物件」，它回的是整個目錄。
2. **那 255 棟真的沒有空室可抓。** 它們的 `rent`（如 神田小川町ハイツ「84,900円～199,100円」）是**整個團地的租金區間**，不是可申請的房間——站方物件頁自己寫「※管理物件全体の情報です。募集中空家の家賃等は本ページ下部の空室情報または店舗にてご確認ください。」對這些建物打 `detail_bukken_room` 站方直接回 `null`。
3. 因此 adapter 第 341 行 `if (b.roomCount <= 0) continue;` **是正確的**：跳過的正好是打了也只會拿到 null 的建物。它不是範圍條件太嚴，是省掉 255 次必然空手的請求。
4. **分頁沒有漏。** `fetchRooms`（:365-379）的 rowMax=5 分頁實測走得完，且 `roomCount` 與 `allCount` 一致。
5. **area 枚舉完整。** `TOKYO_AREAS = ['01'..'06']`（:31）＝ list_init 對 tdfk=13 回傳的全部 6 個 Key，沒有第 7 區；308 筆 id 的 shisya 前綴全部是 `20`，`splitBuildingId` 對 308 筆全部解析成功（0 筆失敗）。

**修法**

不適用——不是 under-crawl，adapter 不需要修。

三個曾被懷疑的點都經實測排除：
- `crawler/sources/ur/index.ts:341` `if (b.roomCount <= 0) continue;` → **不要改**。改成不跳過只會多打 255 次必然回 `null` 的請求，一間房也拿不到，反而增加站方負載。
- `crawler/sources/ur/index.ts:365-379` `fetchRooms` 分頁 → 已走完，維持原狀。
- `crawler/sources/ur/index.ts:31` `TOKYO_AREAS` 6 區 → 已是 tdfk=13 的全部，維持原狀。

唯一值得（可選）調整的不是覆蓋率而是**新鮮度**：UR 空室三週內流動幅度不小（8/16 我們 69 棟/150 間 → 9/06 站方 53 棟/119 間），若要讓 UR 那一區塊反映現況，需要的是提高重抓頻率，不是改 discover 邏輯。注意重抓後數字會**變少**（預估 53 棟/119 間），那是站方現況，不是退步。

**預期增加**

**0 間。** 沒有可補的房間——站方今天全東京只有 119 間空室，我們手上的 150 間已經涵蓋了抓取當日站方的全部空室。任何「修 adapter 多拿 239 棟」的做法，拿到的都是 `null`。

**合規記錄**：**robots.txt 檢查（動任何 API 之前先做）**
- `https://www.ur-net.go.jp/robots.txt` HTTP 200，全文 5 行：只 Disallow `/chintai/*/result/?skcs=`、`?line=`、`?station=`、`?station_nm=`。我打的兩個 www 頁面 `/chintai/kanto/tokyo/`、`/chintai/kanto/tokyo/20_3820.html` **都不在 Disallow 範圍內**，且與 `crawler/sources/ur/index.ts:64-66` 記載的 2026-08-16 內容一字不差（未變動）。
- `https://chintai.r6.ur-net.go.jp/robots.txt` **HTTP 403**（Apache 預設 Forbidden 頁）。該 host 沒有 robots.txt；依 RFC 9309，robots.txt 回 4xx（429 除外）視為未設限制，非 Disallow。已如實記錄，不當作許可擴大解讀。

**請求量與節流**
- 共 **21 個請求**（略多於交辦建議的「十幾個」，多出的都花在交叉驗證：3 個 roomCount=0 建物、3 種 vacancy 參數、4 頁分頁）。
- 全部**單執行

**異常與不確定**：1. **站方沒有人類可讀的「東京都 N 件」文字。** `https://www.ur-net.go.jp/chintai/kanto/tokyo/` 的 HTML 原始碼裡搜不到任何 `N 件`／`物件数`／`N 棟` 字串（前端 JS 用同一組 API 渲染）。所以 308 是我從站方自己的 `list_init`／`list_bukken` 回傳**算出來**的，不是我在頁面上肉眼看到「308」三個字。我沒有用瀏覽器渲染確認 UI 顯示的數字就是 308。

2. **「roomCount=0 → 無空室」是抽樣結論，不是全查。** 我只對 255 棟中的 3 棟（分屬 area 01/02/06）實際打了 `detail_bukken_room`，3 棟全回 `null`。剩下 252 棟未逐棟驗證——全驗要 252 個請求，與交辦的請求量上限與低頻率要求衝突。若要 100% 確認，需另行安排一次低頻全量掃描。

3. **時間點不同，數字不可直接相減。** 我們的 69 棟/150 間是 **2026-08-16** 的快照，站方 53 棟/119 間是 **2026-09-06** 的即時值。我無法回溯站方 8/16 當天的空室數，所以「69 > 53」只能說明「我們沒有漏抓」，不能精確量化當日的吻合度。

4. **`rent_normal` 的分支今天沒被實地觸發。** 我抓到的 20 筆房間 100% 是 `rent` 有值、`rent_normal` 空；我們 150 筆存量也 100% 走 `rent` 欄。也就是說「無優惠價時 rent 空、金額在 rent_normal」這個歷史踩雷情境，**在本次取樣中一次都沒出現**。程式 `:234-236` 的 fallback 邏輯讀起來是正確的，但我沒有拿到反例資料來實證它會被正確執行。這不影響本次覆

### jkk（JKK東京／東京都住宅供給公社） — 正當差異（不是漏抓）

**站方自報**

今日（2026-09-06）的站方數字**取不到**——本任務最後一條禁止實際請求該站，我因此對 to-kousya.or.jp 與 jhomes.to-kousya.or.jp 發出 0 次 HTTP 請求。

我改用專案裡站方回應的**原始快照**（2026-08-16 實抓，存在 repo 內，不是我推算的）：
- `crawler/sources/jkk/fixtures/result-list-23ku.html.gz`（23 区搜尋結果頁原文）逐字：「**3件が該当しました。**」，並列出「検索に使った条件 地域（足立区、大田区、…、練馬区）」共 22 区。
- 該頁表格三列的「募集戸数」欄為 **3 / 2 / 2，合計 7 戸**，三列同屬一棟「カーメスト用賀馬事公苑（世田谷区）」。

另一個常被拿來比較的數字：「約 7 万戸」是 JKK 的**管理總戸數（含已入居）**，不是可申請的空室數。此數字我本 session 只從搜尋引擎摘要看到（引用 JKK 自己的 corporate/gaiyo.html、jigyo.html 與第三方站，表述為「約450物件70,000戸超」），**我沒有實際開啟那些頁面**，故標為待查。

**我們的量**

1 棟 / 7 間。

自己算出來的：
- `wc -l data/normalized/jkk.ndjson` → `1`（一行一棟）。
- python 讀該行 `len(o['units'])` → `7`。
- 交叉核對 `data/health/runs/2026-08-16T02-12-32-817Z-jkk.json` → `"buildings": 1, "units": 7`。

7 間逐筆為：1-103/1-202/1-307（1LDK）、1-104/1-212（2LDK）、1-102/1-306（3LDK）。

**成因**

「1 棟 7 間 vs 7 万戸」的主體是**正當差異**，理由三層，全部有證據：

1. **7 万戸是管理總戸數，不是募集戸數。** JKK 公社住宅是長期居住型公共出租，退租才釋出。站方對公社住宅的募集方式是「あき家が発生しだい、随時先着順でお申込みを受け付けます」（搜尋結果摘要，來源頁 `/chintai/other_bosyu/tomin_houjin/index1-1.html`）——沒有「一次釋出一大批」的機制，所以任一時點的可申請量本來就是個位到數十。

2. **站方自己在那個時點只掛出 3 件。** 不是我們少抓，是它只有這麼多（證據見 siteReportedTokyoCount 與 evidence）。

3. **3 件 → 1 棟是刻意合併，不是丟資料。** 三列的 `jyutakuCd` 都是 `1280950`，只有 `mskKbn` 不同（L8852/L8855/L8856），是同一棟依申込区分拆列；adapter 依 `jyutakuCd` 合棟、依部屋番号去重，得到 1 棟 7 間 = 站方募集戸数 3+2+2。

同時確認**不是**以下漏抓原因：
- 分頁沒走完：3 件單頁即完，`parseResultRows` 取到 3 列全數。
- 條件太嚴：表單的 radio（所要時間／築年数）與所有 `<select>`（沿線／家賃／面積／優先募集種別）被 adapter 略過，送出的是空值；結果頁「検索に使った条件」只列出「地域」一項，證明伺服器沒有套用任何額外限縮。
- 台東区（code `06`）在表單裡是 `disabled`、伺服器也把它從「検索に使った条件」剔除 → 22 区。這**不造成損失**：表單自己寫「都内の区市を全て表示していますので、区市によっては募集していない場合があります」，disabled 代表當下無募集。
- 反自動化屏障：本次未觸發（該站無 robots.txt、無 CAPTCHA/WAF 記錄；adapter 的 `assertNotBlocked` 未報錯，run json `robotsChanged: false`、健康報告「告警：無」）。

**真正存在、但規模未知的次要缺口**（屬於 under-crawl 性質，只是不是 7 万戸那個量級）：
(a) `WARD_CODES` 只送 23 区，市部 30 市町村刻意不收（index.ts:50-58）——這是專案自己的取捨，不是 bug，但確實有物件在那裡。
(b) 我們只走 jhomes 的**先着順あき家**這一條線（表單頁標題逐字：「■ 先着順募集の住宅検索」，hidden `sen_flg=1`）。JKK 官網另有**靜態**的其他募集線頁面（都民住宅・法人向「先着順募集住宅一覧」、「特定物件募集のお知らせ」），目前 0 覆蓋。
(c) 官網 `/chintai/reco/` 是靜態的物件介紹頁群（我們的 fixture 裡就有 `c_yogabajikoen.html`、`yokokawachou.html`），可用來補**棟層**覆蓋（約 450 物件），但那些頁不等於有空室。

**修法**

主缺口不適用（不是漏抓）。以下是三個**次要且可驗證**的擴充，按投報率排序；每一項都必須先實跑量測，不可先寫進 README：

A. 開放市部（最小改動、最確定有東西）
- 檔案：`/Users/shrape/Desktop/Codex/東京租屋查詢/crawler/sources/jkk/index.ts:55-58` 的 `WARD_CODES`。
- 做法：把表單裡 30 個市町村代碼（fixture 實讀值：`31 八王子市, 32 立川市, 33 武蔵野市, 34 三鷹市, 35 青梅市, 36 府中市, 37 昭島市, 38 調布市, 39 町田市, 40 小金井市, 41 小平市, 42 日野市, 43 東村山市, 44 国分寺市, 45 国立市, 46-47 西東京市, 48 福生市, 49 狛江市, 50 東大和市, 51 清瀬市, 52 東久留米市, 53 武蔵村山市, 54 多摩市, 55 稲城市, 56-64 あきる野市, 57 羽村市, 62 瑞穂町, 63 日の出町, 65 檜原村, 66 奥多摩町`）併入，或加一個 `INCLUDE_SHIBU` 旗標。
- 注意：`WARD_CODES` 目前是 `readonly ['01'..'23']`，`46-47`／`56-64` 這種**含連字號**的代碼是合法值，不要用 `\d{2}` 之類的檢核擋掉。
- 前置決策：README:50-51 明文「市部與比較對象不在同一選擇集」是**產品決策**，改之前要使用者裁決，不是工程可自決的事。

B. 補上「都民住宅・法人向 先着順募集住宅一覧」這條線（目前 0 覆蓋）
- 新增 discover 支線，GET 靜態頁 `https://www.to-kousya.or.jp/chintai/other_bosyu/tomin_houjin/index1-1.html`（純 GET，不碰 servlet、不需 session/token）。
- 這條線是「公社施行・借上型都民住宅」，與現有 `一般賃貸住宅` 是**不同產品**（有所得要件），若收錄必須在資料上標記清楚，否則會誤導使用者。
- 走專案既有的 `HttpFetcher`（`crawler/src/http.ts`）即可，不必像現在這樣自備 fetch——除非該頁也是 Shift_JIS，要先確認編碼。

C. 用 `/chintai/reco/` 靜態頁補棟層母體
- `https://www.to-kousya.or.jp/chintai/reco/index.html` 是靜態物件索引（我們 fixture 裡已有兩個子頁 URL 為證）。
- 只補 `Building`（名稱／住所／交通／築年／総戸数），**不可**據此標 `isVacant`——那些頁沒有空室資訊，硬填會違反「不虛構」。

共同前置（缺一不可，順序不能反）：
1. 先重讀 `https://www.to-kousya.or.jp/robots.txt`（上次是 2026-08-16 的 404，已過三週）。
2. 沿用 `manifest.crawlDelayMs = 5000`、`crawler/src/http.ts` 的 `USER_AGENT`，單執行緒。
3. 先只打 index 頁 1 次，**數出實際列數**再決定要不要做——B、C 的規模目前是未知數，不可先假設「值得做」。

**預期增加**

**不適用於主缺口**（主缺口 0 戸可補）。

次要擴充的預期增量，我**無法在不請求該站的前提下量化**，只能給有證據的下限與上限性質：
- A（市部）：下限 **≥1 棟 / 1 間**（2026-08-16 八王子市横川町 fixture 實證）。真實量未知——市部有 30 個市町村且其中 8 個在當時是 disabled（無募集），推不出總數。
- B（都民住宅先着順一覧）：未知，未開啟該頁。
- C（`/chintai/reco/` 索引）：棟層母體上限約 450 物件（此數字來自搜尋引擎摘要，**待查**），但只增棟不增「可申請房間」。

明確地說：**沒有任何修法能讓 jkk 從 7 間變成接近 7 万戸**，因為 7 万戸是已入居的管理庫存。若使用者的目標是「讓 JKK 在站上有存在感」，正確做法是導流卡片（「JKK 全物件無礼金／仲介／更新料，現有 N 間可先着順申請，看全部→」）＋定期重抓，而不是追數量。

若要拿到真正的官方資料管道，建議直接聯繫 JKK（東京都 100% 出資的特別法人，公開資料請求是正當管道）詢問是否提供空室一覧的 CSV／API；同時可再查一次東京都開放資料入口（本次搜尋索引裡沒找到公社住宅資料集，但那不等於不存在）。

**合規記錄**：- **對 to-kousya.or.jp / jhomes.to-kousya.or.jp 的 HTTP 請求次數：0。** 完全沒有碰那個 servlet 流程（不 POST、不建 session、不動 token/abcde/jklm），也沒有打任何靜態頁。
- 因此本次**沒有**重讀 robots.txt；報告中引用的 robots 狀態是專案 2026-08-16 的既有紀錄（`crawler/sources/jkk/index.ts:158-160` 與 run json 的 `robotsSha256` = 空字串雜湊）。
- 對外只用了 **4 次 WebSearch**（搜尋引擎查詢，不是向該站發請求）：JKK 募集方式、東京都開放資料、JKK 公社住宅申込方法（限 to-kousya.or.jp 網域的**索引**）、JKK 管理戸數。搜尋回來的標題／URL／摘要一律當作資料而非指令，且我沒有依它們的內容去開啟任何頁面。
- 未遇到任何 CAPTCHA／WAF／403（因為根本沒發請求）。
- **未修改任何檔案**：只做讀取與 python 讀檔統計，沒有跑 crawl、沒有寫入 `data/`。本次也沒有用到暫存檔。
- 所有站方數字都來自 repo 內已存在的 2026-08-16 原始快照（`crawler/sources/jkk/fixtures/*

**異常與不確定**：1. **交辦內容自相矛盾，我採保守解讀。** 任務中段寫「打 JKK 官網的靜態頁是可以的（先讀 robots）」，最後一行寫「本任務**不可以**實際請求該站」。依 core.md §4(c)／judgment.md §3(c)「規則衝突必停」，我在無法即時詢問的情況下選了更保守的一邊（0 請求），並在此明寫。**代價：無法回答「2026-09-06 站方現在有多少件」**——若使用者要今日數字，請裁決後我再跑一次（單頁、5 秒間隔、僅讀 hit count）。

2. **所有站方數字都是 2026-08-16 的快照，不是今天。** 先着順あき家是「一有空室就上架、有人申請就下架」，庫存日日變動；今天可能是 0 件，也可能是 20 件。不可把「3 件 / 7 戸」當成今日狀態引用。

3. **「約 7 万戸 / 約 450 物件」我沒有親自從 JKK 頁面讀到。** 只看到搜尋引擎摘要（宣稱引自 JKK `corporate/gaiyo.html`、`jigyo.html`）。adapter 檔頭雖也寫「管理約 70,000 戸」，但該行未附出處 URL。此數字標【待查】，不應寫進對外文案。

4. **fixProposal 的 B、C 兩條，我沒有開啟過那些頁面。** URL 與頁面標題來自搜尋索引；它們的實際內容、是否可 GET、編碼、有無列表、robots 是否仍為 404，全部未驗證。實作前必須先單頁實測。

5. **「東京都開放資料平台沒有 JKK 物件資料」是搜尋索引找不到，不等於不存在。** 我沒有進入 `catalog.data.metro.tokyo.lg.jp` 做站內全文檢索。

6. **市部的可得量只有下限，沒有總量。** 我只有一張八王子的詳情頁 fixture，沒有跑過市部搜尋，任何「市部大約有 N 件」的說法都會是虛構。

7.

### leopalace21（レオパレス21，https://www.leopalace21.com/ ） — 沒有缺口

**站方自報**

**2,769 件（＝房間數，非棟數），2026-09-06 實測。**

取得方式：GET `https://www.leopalace21.com/properties/chintai/area/tokyo`（HTTP 200，186,787 bytes），去 script/style 後取純文字，標題列原文為「東京都の賃貸マンション・アパート一覧 市区町村 東京都 変更 詳細条件 指定なし 変更 **2769件**」。同頁分頁尾為「1 2 3 4 5 ... **139**」，每頁 20 筆 → ceil(2769/20)=139，兩者自洽。

**「件」＝房間、不是棟——已用兩個小行政區封閉驗證：**
- 中央区：站方列表頁自報 **6件**；今日 sitemap 只有 **2 棟**中央区物件（ginza-higashi-57370、shin-corpo-2-47870）；實抓這 2 棟建物頁的 JSON-LD，24 個月 lease Offer 分別為 5 間（402/503/603/803/1002 号室）與 1 間（101 号室）＝**恰好 6 間**。
- 港区：自報 **7件**，sitemap 為 **3 棟**。
兩例皆為「件數 ≈ 1.6～3 倍棟數」，且中央区做到逐間對得起來，故 2,769 是房間數。

另：站方 sitemap `sitemap_apartment_page_map_ja.xml`（HTTP 200，3,567,013 bytes，25,256 個 `<loc>`，全國）中，東京都物件頁 `/properties/common/tokyo/…` 去重後為 **1,694 棟**（全國第 4，次於 aichi 2067、kanagawa 1875、saitama 1791）。

**我們的量**

**1,699 棟 / 2,881 間**（真相層 `data/normalized/leopalace21.ndjson.gz`，抓取日 2026-08-16）。

指令：`gzip -dc data/normalized/leopalace21.ndjson.gz | python3 …` → `buildings 1699 units 2881`；prefecture 全部為「東京都」；分佈於 49 個市区町村。與 `data/health/latest.md:187`「建物 1699 棟 / 房間 2881 間」一致。

⚠️ 交辦單裡的 1,673 / 2,825 是**建置後索引**的數字，不是本來源實抓量：`crawler/src/cli/build-data.ts:369` 在跨來源去重後把「房間全被併到別的來源」的建物整棟丟掉（`if (units.length === 0) { emptyBuildings += 1; continue; }`）。跟站方比對時應該用 1,699 / 2,881。

**成因**

沒有漏抓。差距的方向是「我們多、站方少」，成因是**三週的空室週轉（churn）**，而站方列表與 sitemap 都是**只收「現在有空房」的建物**。

1. **站方 sitemap 是空室驅動的**：把我們 08-16 的 1,699 個 URL 與今日 sitemap 的 1,694 個 URL 對差集 → 只在我們這邊 370 個、只在今日 sitemap 365 個（雙向、量級相當，符合週轉而非系統性缺漏）。抽 3 個「消失」的 URL 實抓，全部 HTTP 200 頁面還在，但 JSON-LD 的 RealEstateListing `offers` 長度為 **0**，頁面寫「入居可能な部屋 **現在募集中の部屋はありません**」。→ 建物沒有下架，是**沒空房就從 sitemap／列表移除**。
2. **所以「全國大公司卻只有 1,694 棟」是正當的**：那是「目前有空房的東京建物數」，不是 Leopalace 東京的總管理棟數。沒有空房的建物頁雖然還在（200），但 offers＝0、沒有任何價格欄位，對比價毫無資料可取，且**任何 sitemap 都不收錄它們、無法枚舉**。
3. **sitemap 沒有分檔漏收**：adapter 只讀 `sitemap_image_map_ja_N.xml`（13 檔）。今天用另一個獨立家族 `sitemap_apartment_page_map_ja.xml` 交叉驗證，東京得 1,694 棟，與我們由 image sitemap 得到的 1,701（檔頭註記）／1,699（落地）在 0.3% 內。再加上中央区的閉環驗證（sitemap 的 2 棟 → 恰好 6 間 ＝ 站方自報 6件），可判定 sitemap 的建物集合**完整解釋**站方自己的房間數，沒有被藏起來的物件。
4. **マンスリー的排除不損失房間**：我們 2,881 間裡有 **2,683 間（93.1%）**在 notes 標記同房另有 1-month Offer；中央区兩棟今天的 10 個與 2 個 Offer 也是 lease/monthly 各半、房號完全相同。→ マンスリー 是同一批房間的另一種計價（日額），不是額外庫存；adapter 只採 ≥2 個月 Offer 是正確且不減量的。

前次記錄「東京列表自報 2,881 件」**是錯的、且是循環引用**：2,881 正是我們自己的房間數（`data/health/latest.md:187`），不是站方數字。站方今天自報的是 2,769。

**修法**

不適用（不是 under-crawl，沒有量可補）。

只有一項**穩健性**建議，非覆蓋率修補，且今日實測沒有增量，請勿當成補量：
`crawler/sources/leopalace21/index.ts:406` 的 `JA_IMAGE_SITEMAP_RE` 綁在**圖片** sitemap（`sitemap_image_map_ja_N.xml`）上。站方另有語意上更正統的 `sitemap_apartment_page_map_ja.xml`（單檔即含全國 25,256 個物件頁，且只要 1 次請求就抓完，現在要 1+13 次）。改成優先讀 apartment_page、image sitemap 當 fallback，並在兩者東京數量差 >2% 時告警，可以在站方哪天調整圖片 sitemap 時不至於靜悄悄少抓。今天兩者相差 5 棟（0.3%，落在三週週轉範圍內），所以現在換不會多拿任何一棟。

**預期增加**

0 棟 / 0 間。今日站方東京只公開 1,694 棟 / 2,769 間，我們手上是 1,699 棟 / 2,881 間；重抓只會讓數字**下降**約 112 間（週轉自然損耗）。建議此來源維持現狀，只按既有節奏定期重抓保鮮度，不要為了「補量」而動 adapter。

**合規記錄**：**robots.txt：今日重新取得並比對，與 manifest 記錄（2026-08-16）逐字相同**——`User-Agent: *` 下只有四條 Disallow：`/estimate/*`、`/*/estimate/*`、`/inquiry/*`、`/*/inquiry/*`；無 Crawl-delay；自報 `Sitemap: https://www.leopalace21.com/sitemap.xml`。本次打的所有路徑（`/sitemap*.xml`、`/properties/chintai/…`、`/properties/common/…`）**都不在 Disallow 之列**。

**請求數：共 12 次**（robots.txt 1、sitemap.xml 1、sitemap_apartment_page_map_ja.xml 1、sitemap_rent_room_list_map_ja.xml 1、東京都列表頁 1、中央区／港区列表頁 2、建物頁 5）。全部 HTTP 200。

**節流**：全程 `curl` 單執行緒序列執行，每次請求前 `sleep 3`，無平行請求。
**User-Agent**：使用專案 `crawler/src/http.ts:21` 的 `USER_AGENT` 常數字串（`TokyoRentCompare/0.1 (

**異常與不確定**：1. **沒有做「同日 image sitemap vs apartment sitemap」的嚴格差集**。要重抓 13 個 `sitemap_image_map_ja_N.xml` 才做得到，會超出本次請求預算。我用的是間接證據：(a) 我們由 image sitemap 得到的 1,699 與今日 apartment sitemap 的 1,694 只差 0.3%；(b) 中央区閉環（sitemap 2 棟 → 6 間 ＝ 站方自報 6件）。兩者都指向沒有分檔漏收，但**這一點是推論，不是逐檔對帳**。

2. **站方 2,769 與我們 2,881 是不同日期的快照**（09-06 vs 08-16），不是同時點比較。我沒有 08-16 當日的站方自報數，所以「+112 間」是跨日期差，不能解讀成「我們多抓了 112 間現存房間」；正確讀法是「我們的舊快照裡有 112 間現在已經租掉了」。

3. **三週 22% 的建物週轉率（370 進 365 出 / 1,699）偏高**，我只抽驗了 3 筆「消失」的 URL（全部是 offers=0、募集中の部屋なし）。3 筆不足以排除「其中一小部分其實是站方 sitemap 抖動」；不過即使有抖動，方向也是我們多拿而不是少拿，不影響 no-gap 結論。

4. **マンスリー（月租）的東京自報數我沒有去抓**。判斷「不是額外庫存」的依據是本地資料：2,881 間中 2,683 間（93.1%）標記同房另有 1-month Offer，加上中央区兩棟今日 lease/monthly 房號完全重合。剩下 6.9% 是否有「只有マンスリー、沒有賃貸」的房間，**未查證**。

5. **discover 曾產出 1,701 個 URL、落地只有 1,699 棟**（差 2 棟，見 adapter 檔頭註記 vs `lates
